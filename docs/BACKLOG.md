# KnownFirst internal backlog

A compact internal backlog for solo development. GitHub Issues are deferred until collaboration or external testing makes them useful; this file replaces them for now.

## Table

| ID | Priority | Area | Summary | Status | Release blocker | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| KF-LEARN-001 | P2 | Learning UX | Distinguish intentional Again repeats and opposite card directions in the Learn UI. | Implemented on feature branch | No | Investigation confirmed no accidental backend duplication; the Learn card view and page now surface `IsAgainRepeat` and card direction so a legitimate repeat/opposite-direction card is visually distinct from a first-time card. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-LANG-001 | P1 | Text import / Lexical lookup | `TextReviewService` incorrectly re-validated `ExplanationLanguage`/`TargetLanguage` against a local English/German-only set, rejecting valid German-to-Russian and English-to-Russian imports even though `LexicalLookupLanguagePolicy` already supports Russian as a translation target. | Implemented on hotfix branch | Yes (blocks Beta 11 Russian-translation-target claim) | Fixed by removing the duplicated validation and relying solely on `LexicalLookupLanguagePolicy.Validate`. Also removed the combined Definition-and-Translation lookup choice from new imports (`DefinitionAndTranslation` remains supported for existing/legacy data only). See `docs/CURRENT_WORK.md`. |
| KF-UX-001 | P2 | Settings / Portable recovery | Add the standard vertical spacing to the portable-import confirmation and hide or disable the original Export and Import actions until Cancel or Import completes. | Implemented on hotfix branch | No | The Data Export/Import action row is now hidden entirely while the import confirmation panel is visible, restored on Cancel, validation failure, or completed import. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-STATE-001 | P2 | Navigation / Portable recovery | Refresh Home and burger-menu workflow availability immediately after a successful portable import. Cancelled and failed imports must not change state. | Implemented on hotfix branch | No | Added `IWorkflowChangeNotifier`; `NavMenu` and `Home` subscribe and reload `WorkflowState` on change. Published only after a successful import or a successful full data reset. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-EXPORT-001 | P1 | Data safety / Android | Android portable export reported failure after a successful write because `AndroidPortableArchiveFileService` verified the saved destination via `Stream.Length`, which non-seekable Android content-provider streams can throw on. | Implemented on hotfix branch | Yes (blocked reliable Android export confirmation) | Root cause is non-seekable destination verification, not archive corruption. Fixed with a bounded single-byte read (`PortableArchiveExportGuard.VerifySavedArchiveAsync`) that never touches `Length`/`Position`. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-CONSENT-001 | P2 | Settings / Privacy | Settings had no way to grant online-lookup consent independently of the Prepare-words flow; users who never triggered a lookup, or who restored/reinstalled, had no activation path. | Implemented on hotfix branch | No | Settings now shows the binding online-lookup disclosure and an explicit "Activate online dictionary" action calling `IAppSettingsService.GrantOnlineLookupConsent` when consent is absent. Portable archives still never export/import consent. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-BACKUP-002 | P1 | Data safety / Portable recovery | Non-destructive populated-target merge of `.kfarchive` into installations that already have data; enables realistic PC-and-phone divergence recovery. | Slices 1–9, Package A, Package B, and Package C merged | This initiative no longer blocks public release readiness | Design complete ([architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md)). Slices 1–9 merged on master (PRs #26–#28, #31–#33, #40–#45), completing contracts library, safety copy, preflight, activation, merge planning, import preview, and transactional populated-target merge writer. Package A (Schema-9 completed-review convergence identity and planner) is merged (PR #52). Package B (writer evidence) is merged via PR #65. Package C (completed-`ReviewSession` and `SourceMaterial` cross-installation canonical ordering, two-installation convergence evidence) is merged via PR #68 (merge commit `db47de3bf48b49b5258ce16acc6e3e543d96143c`). The non-destructive populated-target merge initiative's completion conditions are now satisfied on master. |
| KF-SYNC-001 | P3 | Data safety / Sync | Optional future synchronization transport reusing the exact archive merge engine (KF-BACKUP-002); no KnownFirst server exists today. | Deferred, not implemented | No | Purely a future possibility to record: if a sync transport is ever built, it should reuse the same content-derived merge identities and conflict policies rather than a separate protocol. No design, code, or transport exists yet. |
| KF-LEX-002 | P3 | Lexical lookup / Providers | Investigate Duden API suitability as an additional/alternative lexical source. | Deferred, not implemented | No | The Duden free plan currently documents 20 requests/day and spelling, grammar, punctuation, and synonym functions; whether it can supply dictionary-style definitions suitable for KnownFirst's Meaning content is unverified. Needs a dedicated investigation before any provider-integration design. |
| KF-LEX-003 | P3 | Text import / Lexical analysis | English multiword-term recognition (e.g. `Large Language Model`) and lexical phrase identity. | Deferred, not implemented | No | English multiword lexical and technical phrases (such as `Large Language Model`, `machine learning`, `natural language processing`) should later be recognized as single lexical review identities when enhanced term recognition is active. The whole phrase and its individual component words are distinct lexical identities. If `large`, `language`, and/or `model` are already established/known in vocabulary state, those component words must not be re-asked merely because the phrase is recognized. Knowing all component words does not imply that the user knows the phrase concept itself (the recognized phrase may still be reviewable/learnable as its own identity). Future acronym linkage such as `Large Language Model` ↔ `LLM` remains part of this deferred English work. English multiword recognition is explicitly deferred from the initial German compound-term release. |
| KF-TRANS-001 | P3 | Lexical lookup / Providers | Investigate Google Cloud Translation (or an equivalent translation API) as a future provider. | Deferred, not implemented | No | Must never hardcode a shared API key; any implementation must use user-supplied credentials or a future backend proxy. No design or code exists yet. |
| KF-LEARN-002 | P3 | Learning UX | Later change the normal daily learning choices from their current values to 5/10/15/20, with a proposed default of 5 and a normal maximum of 20. | Deferred, not implemented | No | Product-parameter change only; not started. |
| KF-PROVENANCE-001 | P2 | Data integrity / Provenance | Verify manual Definition/Translation attribution: manually authored content must never be labeled as Wikimedia-sourced, and provider-origin content that a user later hand-edits must remain distinguishable from purely manual entries. | Deferred, not implemented | No | Needs a dedicated review of `MeaningEntity`/`BackupSourceReference` provenance fields and the editing flows that can mutate `Definition`/`Translation` after import; no code change made as part of KF-BACKUP-002 Slice 3. |
| KF-MEANING-001 | P1 | Learning model / Data safety | Meaning-centric cards and answer-variant mastery: Schema-9 cards and queues are Sense-addressed; Schema 9 was activated for this initiative and was later superseded by Schema 10. | Slices 1–9, Package A, Package B, and Package C merged | The Package-C prerequisite is satisfied, but independent release blockers remain | Slices 1–9 merged on master (PR #45). Slice 6 set `DatabaseSchema.CurrentVersion` to 8 and activated the migration. Slice 8 implemented `PortableMergeWriter`. PR #51 activated Schema-9 review-session history storage. PR #52 merged Package A. Package B (writer evidence) is merged via PR #65. Package C (convergence hardening) is merged via PR #68 (merge commit `db47de3bf48b49b5258ce16acc6e3e543d96143c`). The Package-C public-release prerequisite for this initiative is now satisfied. |
| KF-RELEASE-001 | P2 | Release tooling | Revalidate the canonical Google Play AAB workflow before the next genuine release; the repository defect was diagnosed and corrected in PR #101, but runtime packaging revalidation remains outstanding. | Repository correction merged (PR #101); runtime AAB workflow revalidation deferred until authorized release preparation | Yes for next AAB; no for feature development | In `scripts/publish-google-play-bundle.ps1`, `$ErrorActionPreference = "Stop"` caused `Get-ChildItem` to throw `PathNotFound` when `bin\Release\net10.0-android` did not exist after `dotnet clean`. Corrected to initialize `$preCandidates = @()` and check `Test-Path -PathType Container`, while preserving fail-closed behavior when stale `*-Signed.aab` files remain. Backed by isolated behavioral regression tests in `AndroidPublishingScriptContractTests.cs`. Merged via PR #101 (merge commit `b1e303b8cb329276f407e1f0c75f4103749465a4`). Runtime AAB packaging execution remains outstanding and deferred to authorized `PACKAGE_ONLY` release execution. |
| KF-BACKUP-003 | P3 | Data safety / Portable recovery | Historical Package-D ordering correction; its original residual statement remains a package-boundary fact. | Merged via PR #76 | No | PR #87 later merged `Learning.Cards`; PR #88 completed its documentation closure. The distinct Occurrence action-key correction is now merged and binding through PR #89 (implementation `edbb49a87ff3f37337c413111a60f6cfa6805b88`, feature head `d45a7e8fad533ddda5dda425356bf2095e8bafb6`, merge commit `49d25cb8d7d113d1f0b1826369d9105a37d9207b`). It replaces colliding `SentenceId:VocabularyId` with the shared lookup-only `SourceMaterialArchiveId:Occurrence.Order` key; bounded TEST_ONLY was 257/0/0 and independent review found 0 BLOCKER / 0 MAJOR / 0 MINOR / 0 NIT. |
| KF-BACKUP-004 | P3 | Data safety / Portable recovery | Schema-9 populated-target `LearningReview` merge integrity; merged historical package. | Merged via PR #77 (merge commit `bec861fb8a054beb2804f1132b450da1e45dee90`); `POST_MERGE_SYNC_ONLY` complete | No | Its `CardId@ReviewedAtUtc` defect was corrected in the V2 path. The direct legacy planner's analogous label can collide but is not production-writer reachable because V1 input upgrades to V2 and stale-plan validation recomputes V2. It is not a current production-writer defect; the distinct Occurrence action-key correction completed Priority 15 through merged PR #89. |
| KF-BACKUP-005A | P2 | Data safety / Portable recovery | Schema-10 stable learning-workflow identity foundation. Introduces immutable `StableId` columns for `LearningSessions` and `LearningSessionCards`. Deterministic SHA-256 64-char Completed session/queue bootstrap; one-time GUID 32-char Active session/queue bootstrap; StableId immutability enforced through all subsequent workflow mutations. Archive/source compatibility: source ≤9 Completed portable workflows may receive bootstrap StableIds; source ≤9 Active portable workflows remain unsupported/rejected; source ≥10 workflows require valid canonical StableIds. `LearningSessionId` exclusion from `LearningReview` merge identity (KF-BACKUP-004) preserved unchanged. **Active portable-workflow continuation is explicitly excluded from this scope.** | Merged via PR #79 (merge commit `e56b8bfa27dfe1d630fbacfed24e6d56ea876026`); `POST_MERGE_SYNC_ONLY` complete | No | Evidence: candidate `ValidateAll` passed **FULL GREEN** (`ALL_AUTOMATED` **1812 passed / 0 failed / 0 skipped**; Windows Debug/Release passed; Android Debug/Release passed; 0 build errors; 0 AOT/trimming/source-gen warnings; 8 non-blocking XML-doc warnings). Later commits after candidate checkpoint `551399df...` were documentation-only with no executable/test-tree difference. Succession: KF-BACKUP-005B (portable Active workflow export/restore into empty target) and KF-BACKUP-005C (populated-target Active workflow convergence and conflict safety) follow after 005A. See [architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md) §23 and [DATABASE_CONTRACT.md](DATABASE_CONTRACT.md) Schema-10 contract. |
| KF-BACKUP-005B | P2 | Data safety / Portable recovery | Schema-10 portable Active learning-workflow export and restore into an empty installation from the last durably committed application/database state. Transports the Active `LearningSession`, persisted queue state, committed mid-session `LearningReview` rows, and unchanged workflow/queue `StableId` values; restores the workflow as Active, remaps reviews to the new local integer session ID, and resumes through normal `LearningService` behavior without fabricating completion. Schema-8/9 remain Completed-only; source ≤9 Active workflows, Active `VocabularyReview`, and Active `PreparationBatch` remain unsupported. | Merged via PR #81 (feature commit `e8236bba3d23e942014e6979b661e0c77a2a3bdd`, merge commit `dc56e8412966ac32531c4b0358526582702d6d24`); `POST_MERGE_SYNC_ONLY` complete | No | Final exact-tree focused evidence: `BackupArchiveV2Tests`, `BackupModelContractTests`, `Schema8BackupRestoreTests`, `BackupServiceImportRoutingTests`, `MergePreflightServiceTests` — **135 passed / 0 failed / 0 skipped**, normal completion, 0 build warnings, 0 build errors, pre/post `git diff --check` passed; final independent PR review: **0 BLOCKER / 0 MAJOR / 0 MINOR**. No GitHub CI evidence existed for the 005B head. Earlier supplementary test-project evidence: **1820 passed / 0 failed / 0 skipped** against the same production implementation before final acceptance-test additions. Schema 10 and archive V2 remain unchanged. Completed Schema-10 workflow portability was regression-tested. At the historical 005B package boundary, populated-target Active preview was `Blocked` and import was `Failed` with `ActiveWorkflowUnsupported`, with zero mutation and no executable merge/writer behavior. Merged KF-BACKUP-005C now supplies bounded populated-target Active convergence; remaining Priority-15 residuals stay open. No 005B `ValidateAll`, platform/Release build, rendered GUI, device/emulator, APK/AAB, signing, publishing, or distribution evidence. See [architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md) §24 and [DATABASE_CONTRACT.md](DATABASE_CONTRACT.md) Schema-10 contract. |
| KF-BACKUP-005C | P2 | Data safety / Portable recovery | Populated-target Schema-10 Active learning-workflow convergence and conflict safety. A quiescent populated target can additively import an Active workflow; exact same-`StableId` durable state is `NoChanges`; any divergent same-`StableId` state, including LearningReview multiplicity mismatch, requires a non-executable conflict decision with zero mutation. | Merged via PR #83 (merge commit `bed54d01624e80ca6dd5adf8af097e64fe33e588`); `POST_MERGE_SYNC_ONLY` complete | No | Binding `master` behavior: Schema 10 and archive V2 unchanged; no new merge engine, DTO format, public status/error code, UI, or sync transport. Existing safety-copy and stale-plan writer safeguards remain binding. Evidence: `Schema10ActiveArchive` **8 passed / 0 failed / 0 skipped**; controlled affected scope **254 passed / 0 failed / 0 skipped** with Workers=1 and **254 passed / 0 failed / 0 skipped** with Workers=8; final relevant reviews **0 BLOCKER / 0 MAJOR / 0 MINOR**. H1/H2 safety-copy-count observations remain unexplained historical transients, non-reproducible, not passing runs, and not fixed by a product-code change. No ALL_AUTOMATED, ValidateAll, GitHub CI, platform/runtime, Release-build, rendered GUI, device/emulator, APK/AAB, signing, publishing, or distribution evidence. |
| KF-UX-002 | P2 | Onboarding / Release notes | Fresh install / full reset must not show the current version's What's New notice as though it were an upgrade. | Deferred, not implemented | No | Intended semantics: upgrade → show the current version's What's New once; same-version subsequent start → do not show it; full reset / fresh-user state → do not show the current version's What's New as though it were an upgrade; a later genuine upgrade must still show that later version's What's New once. Refines the existing one-time notice/reopenable-history contract (see [PROJECT_STATE.md](PROJECT_STATE.md) "one-time localized What's New notice" and [UI_UX_ACCEPTANCE.md](UI_UX_ACCEPTANCE.md) `/release-notes` history). No design or code exists yet for the full-reset distinction. |
| KF-ATTR-001 | P3 | Legal / Data provenance | Future first-start / open-data transparency surface and a Settings/About "Open Source / Data Sources" area. | Deferred, not implemented | Yes (contributes to Public-Release Readiness, Priority 20 in [ROADMAP.md](ROADMAP.md)) | Must attribute German Morphological Dictionaries (`DuyguA/german-morph-dictionaries`, CC BY-SA 4.0; see [PROJECT_STATE.md](PROJECT_STATE.md) "German Enhanced Term Recognition — Packages 1–4 (Merged Production State)" and `KnownFirst.Core/Text/German/Assets/NOTICE.md`) and Wikipedia/Wiktionary, and link the KnownFirst GitHub repository from within the app. Must keep offline German lexical data clearly distinguished from consent-gated online lookup providers. No design or code exists yet. |
| KF-GERMAN-5A-2 | P2 | Data safety / Portable recovery | German Package 5A-2: portable/cross-installation transport semantics for retained derived-term provenance/context. | Merged via PR #137 (merge commit `5d1d3c05bae6ab9f1c56d8c5f9a227121f432f9a`); `POST_MERGE_SYNC_ONLY` complete | No | Cross-installation transport of `DerivedTermEvidence` is implemented through portable archive export/restore and populated-target merge, using the existing content-derived merge-identity approach (no SQLite/archive-local ids) rather than a separate protocol; no schema/archive-format change. Validated PR head `2ff447e9f874d49e72fee0a549820adc1bdc3b39`; exact-head `FULL_VALIDATION` 2248 passed / 0 failed, all four Windows/Android Debug/Release build gates PASS, exit code 0. Lifecycle complete through `POST_MERGE_SYNC_ONLY`. See [ROADMAP.md](ROADMAP.md) Priority 19b, [CURRENT_WORK.md](CURRENT_WORK.md), [PROJECT_STATE.md](PROJECT_STATE.md), and [DATABASE_CONTRACT.md](DATABASE_CONTRACT.md) "Schema-11 Derived-Term Evidence Contract." |
| KF-GERMAN-5B | P2 | Review UX | German Package 5B: visible derived-source context in the Review Words UI. | Implemented on feature branch, under `DOCUMENT_ONLY` reconciliation — not yet committed, pushed, or merged | No | Minimal Review Words source-compound indication implemented, using the already-existing `ReviewCandidateDetails.Provenance`/`DerivationEvidence` projection (no new service/query/database boundary; no schema/archive change). Native Exclude cleanup and derived Preparation→Learning continuity are now characterized by new focused tests, closing the remaining approved German Known-suppression/Preparation/Learning regression-coverage gaps not already covered by Package 5A's review-added tests. `IMPLEMENT`, focused verification (163 passed / 0 failed), and independent `REVIEW_ONLY` (0 BLOCKER / 0 MAJOR / 2 non-blocking MINOR / 1 NIT) are complete on local branch `feature/german-package-5b-review-words-derivation-indication-v1`. Merge lifecycle (`COMMIT_ONLY` through `POST_MERGE_SYNC_ONLY`) remains pending. See [ROADMAP.md](ROADMAP.md) Priority 19c and [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-UX-003 | P3 | Settings | Later simplify/reorganize Settings; consider an Advanced Settings section for rarely used options. | Deferred, not implemented | No | Product-parameter/UX change only; not started. |
| KF-UX-004 | P3 | Review/edit UX | Reduce expert/internal fields exposed to ordinary users in review/edit flows while preserving the richer internal model. | Deferred, not implemented | No | Present a simpler front-of-house mental model where appropriate without discarding the underlying detailed data model. Not started. |
| KF-RELEASE-002 | P2 | Release tooling / Windows | Complete real Windows packaging/distribution validation (clean-PC portable execution, MSIX signing/installability, Store onboarding) before any public release. | Partially evidenced, not complete | Yes (Public-Release Readiness, Priority 20 in [ROADMAP.md](ROADMAP.md)) | A real self-contained Windows Portable ZIP has been produced and structurally verified (see [PROJECT_STATE.md](PROJECT_STATE.md)); clean-PC execution, MSIX packaging, and external distribution remain unexecuted. Deferred behind the German Enhanced Term Recognition priority; resumes only with explicit user authorization. |
| KF-SUPPORT-001 | P3 | Product / Business | Later investigate a one-time voluntary-support ("buy me a coffee"-style) flow with no feature benefit or entitlement. | Deferred, not implemented | No | Requires legal/tax/business/platform review before implementation; must not be labeled a tax-deductible donation by assumption. Currently absent from production rendering (see [PROJECT_STATE.md](PROJECT_STATE.md) "Support KnownFirst"). No design or code exists yet. |
