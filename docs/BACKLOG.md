# KnownFirst internal backlog

A compact internal backlog for solo development. GitHub Issues are deferred until collaboration or external testing makes them useful; this file replaces them for now.

## Table

| ID | Priority | Area | Summary | Status | Release blocker | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| KF-LEARN-001 | P2 | Learning UX | Distinguish intentional Again repeats and opposite card directions in the Learn UI. | Implemented on feature branch | No | Investigation confirmed no accidental backend duplication; the Learn card view and page now surface `IsAgainRepeat` and card direction so a legitimate repeat/opposite-direction card is visually distinct from a first-time card. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-LANG-001 | P1 | Text import / Lexical lookup | `TextReviewService` incorrectly re-validated `ExplanationLanguage`/`TargetLanguage` against a local English/German-only set, rejecting valid German-to-Russian and English-to-Russian imports even though `LexicalLookupLanguagePolicy` already supports Russian as a translation target. | Implemented on hotfix branch | Yes (blocks Beta 11 Russian-translation-target claim) | Fixed by removing the duplicated validation and relying solely on `LexicalLookupLanguagePolicy.Validate`. Also removed the combined Definition-and-Translation lookup choice from new imports (`DefinitionAndTranslation` remains supported for existing/legacy data only). See `docs/CURRENT_WORK.md`. |
| KF-UX-001 | P2 | Settings / Portable recovery | Add the standard vertical spacing to the portable-import confirmation and hide or disable the original Export and Import actions until Cancel or Import completes. | Implemented on hotfix branch | No | The Data Export/Import action row is now hidden entirely while the import confirmation panel is visible, restored on Cancel, validation failure, or completed import. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-STATE-001 | P2 | Navigation / Portable recovery | Refresh Home and burger-menu workflow availability immediately after a successful portable import. Cancelled and failed imports must not change state. | Implemented on hotfix branch | No | Added `IWorkflowChangeNotifier`; `NavMenu` and `Home` subscribe and reload `WorkflowState` on change. Published only after a successful import or a successful full data reset. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-EXPORT-001 | P1 | Data safety / Android | Android portable export reported failure after a successful write because `AndroidPortableArchiveFileService` verified the saved destination via `Stream.Length`, which non-seekable Android content-provider streams can throw on. | Implemented on hotfix branch | Yes (blocked reliable Android export confirmation) | Root cause is non-seekable destination verification, not archive corruption. Fixed with a bounded single-byte read (`PortableArchiveExportGuard.VerifySavedArchiveAsync`) that never touches `Length`/`Position`. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-CONSENT-001 | P2 | Settings / Privacy | Settings had no way to grant online-lookup consent independently of the Prepare-words flow; users who never triggered a lookup, or who restored/reinstalled, had no activation path. | Implemented on hotfix branch | No | Settings now shows the binding online-lookup disclosure and an explicit "Activate online dictionary" action calling `IAppSettingsService.GrantOnlineLookupConsent` when consent is absent. Portable archives still never export/import consent. See [CURRENT_WORK.md](CURRENT_WORK.md). |
| KF-BACKUP-002 | P1 | Data safety / Portable recovery | Portable import today is restricted to an empty target (`TargetNotEmpty` refusal); there is no non-destructive way to merge a `.kfarchive` into an installation that already has data, which blocks realistic PC-and-phone divergence recovery. | Designed, not implemented (revision 2) | Blocks public release readiness; does **not** block Beta 12 Internal Testing | Full design in [architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md): code-verified entity identity (Meaning allows multiple rows per word; LearningCard identity is `(WordId, Direction)`, DB-enforced; Document identity includes lookup mode and target language, not content hash or title alone), explicit non-destructive conflict matrices for every enum, a mandatory pre-merge safety copy (merge does not start if it fails), SHA-256 event fingerprints with an explicit clock-skew policy, and concrete idempotency proof sketches rather than an asserted claim. Acceptance for implementation requires: every §5 conflict-matrix cell and every §11 idempotency proof passing as a test (design doc §12 slice 1) before any database mutation code is written, and the mandatory safety copy (§8) landing before the merge writer (§12 slice 2 precedes slice 5). Four items remain open for product/engineering sign-off (see design doc §13). |
| KF-SYNC-001 | P3 | Data safety / Sync | Optional future synchronization transport reusing the exact archive merge engine (KF-BACKUP-002); no KnownFirst server exists today. | Deferred, not implemented | No | Purely a future possibility to record: if a sync transport is ever built, it should reuse the same content-derived merge identities and conflict policies rather than a separate protocol. No design, code, or transport exists yet. |
| KF-LEX-002 | P3 | Lexical lookup / Providers | Investigate Duden API suitability as an additional/alternative lexical source. | Deferred, not implemented | No | The Duden free plan currently documents 20 requests/day and spelling, grammar, punctuation, and synonym functions; whether it can supply dictionary-style definitions suitable for KnownFirst's Meaning content is unverified. Needs a dedicated investigation before any provider-integration design. |
| KF-TRANS-001 | P3 | Lexical lookup / Providers | Investigate Google Cloud Translation (or an equivalent translation API) as a future provider. | Deferred, not implemented | No | Must never hardcode a shared API key; any implementation must use user-supplied credentials or a future backend proxy. No design or code exists yet. |
| KF-LEARN-002 | P3 | Learning UX | Later change the normal daily learning choices from their current values to 5/10/15/20, with a proposed default of 5 and a normal maximum of 20. | Deferred, not implemented | No | Product-parameter change only; not started. |
| KF-PROVENANCE-001 | P2 | Data integrity / Provenance | Verify manual Definition/Translation attribution: manually authored content must never be labeled as Wikimedia-sourced, and provider-origin content that a user later hand-edits must remain distinguishable from purely manual entries. | Deferred, not implemented | No | Needs a dedicated review of `MeaningEntity`/`BackupSourceReference` provenance fields and the editing flows that can mutate `Definition`/`Translation` after import; no code change made as part of KF-BACKUP-002 Slice 3. |
| KF-MEANING-001 | P1 | Learning model / Data safety | Meaning-centric cards and answer-variant mastery: the live schema stores one `LearningCard` per `(WordId, Direction)` and automatic-progression counters on `WordEntity` (word-level), which cannot represent independent progress per `SemanticMeaning` or per-`AnswerVariant` mastery streaks — verified in [architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md) §16.4. **Also verified: archive format v1 itself cannot encode two `LearningCard` entries for one `(VocabularyId, Direction)` pair even when `PreparedItemId` differs** (`BackupArchiveWriter.ValidatePayloadGraph`'s `cardKeys` uniqueness check, §16.4/§17) — the migration must cover the archive format, not only the live schema. **Slice 0 (binding architecture decision, branch `feature/meaning-centric-architecture-v1`) is complete, including two focused correction passes:** [architecture/meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) decides Option B (`WordEntity` → `SenseEntity` → `MeaningEntity`/exact variants → `AnswerVariantEntity` → `AnswerVariantProgressEntity`) as binding, not provisional; defines the full Schema 8 entity model (corrected: `WordEntity.Status` no longer conflates authoritative word-review state with a Sense-learning rollup — it is reset to `UnknownBacklog` on migration and never written with `Prepared`/`Learning`/`Mastered` again; `WordEntity.PreparationState` is resolved as a technical workflow/cache field only, never semantic-completeness state; `AnswerVariantEntity` is now a pure normalized answer expression with no `Requirement`/preferred field of its own — a new `SenseAnswerVariantAssignmentEntity`, unique by `(SenseId, CardDirection, AnswerVariantId)`, owns `Requirement` (`Required`/`AcceptedOnly`) and `IsPreferred` per direction, since the same variant can be required in one direction, merely accepted in another, and absent from a third; `SenseEntity.DefaultMeaningId` is a non-authoritative fallback while each `LearningCardEntity.PreferredMeaningId` (migrated in place from the legacy `MeaningId` column, upgraded from legacy metadata to authoritative) is the real per-direction preferred-Meaning reference; `AnswerVariantProgressEntity` is unique by `(CardId, AnswerVariantId)`, not `AnswerVariantId` alone; `LearningReviewEntity`/`LearningSessionCardEntity` gained `TargetAnswerVariantId`/`MatchedAnswerVariantId` fields with a defined, assignment-aware synonym-credit rule; a three-tier stable-identity taxonomy (local numeric ID / content-derived matching key / immutable `StableId`) was added), the Schema 7→8 migration contract (an explicit 9-step atomic order, grouping ambiguity that never blocks startup, referential-corruption fail-closed rules kept distinct from grouping ambiguity, idempotency, rollback), the archive v1↔v2 compatibility contract (manifest version bump only, `.kfarchive` retained), and an 8-slice implementation sequence (§6 of that document). No schema, entity, service, archive, or UI code has been changed by Slice 0. | **Slice 0 (architecture, corrected twice) complete; Slices 1–8 (implementation) not started** | **Required before the merge writer** (design doc §12 slice 5 must not ship until this lands) | Acceptance requirements: permit multiple SemanticMeanings per Word; card identity becomes `(SemanticMeaningIdentity, Direction)`; **a compatible archive format revision** so a device holding two SemanticMeaning cards for one Word/Direction can still export a valid, round-trippable archive; independent learning progress per meaning; direction-specific preferred-answer/requirement assignment (`SenseAnswerVariantAssignmentEntity`: `Requirement` `Required`/`AcceptedOnly` plus `IsPreferred`, scoped per `(Sense, CardDirection, AnswerVariant)`) with no card created for every optional alias; per-required-variant reading and typing streaks; configurable success threshold, initially preserving the current value of two; queue interleaving that avoids presenting the same (Word, SemanticMeaning, AnswerVariant) immediately twice while another due item exists; correct handling when the user types a different accepted synonym (semantically correct, does not satisfy the specifically targeted variant, which is rescheduled); user-selected topic/domain and context association (topic is not persisted today — see §16.6); migration of existing `WordEntity` automatic counters with existing progress preserved, not discarded; archive/import/merge/convergence tests against the migrated schema. Full mastery contract recorded in design doc §16.3 and restated as binding in [meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) §2.6/§3. |
