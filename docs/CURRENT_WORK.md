# KnownFirst Current Work

## Last updated

2026-08-25 (Neutral post-merge state on master@d8e14a699ef52030d542309015064d8ac0668508 following manual merge and POST_MERGE_SYNC_ONLY of PR #156; Schema is 12; Archive format is V2).

## Repository and Worktree Governance

- Repository: https://github.com/Tachiguro/KnownFirst.git
- Canonical local working directory: Exactly one canonical local checkout and one normal worktree per environment (defaulting to `C:\Dev\KnownFirst`, see [ADR-0007](decisions/ADR-0007-single-canonical-working-directory.md)).
- Single writer: Only one writing agent operates at a time in the repository.
- Single worktree: Additional worktrees or repository copies require explicit user authorization.

## Mandatory Documentation Governance

Every repository-writing package follows the governed multi-slice lifecycle: `PLAN_ONLY` → slice loop (`IMPLEMENT_SLICE` or focused TDD RED/GREEN → `CHECKPOINT_COMMIT_ONLY`) → consolidated `REVIEW_ONLY` → mandatory package-level `DOCUMENT_ONLY` → candidate finalization / `COMMIT_ONLY` (if documentation edited) → exact-candidate-HEAD `FULL_VALIDATION` → `PUSH_ONLY` → `PR_ONLY` → manual user merge → `POST_MERGE_SYNC_ONLY`. Documentation-only packages omit `IMPLEMENT`. See [docs/PROMPT_AND_TASK_ROUTING.md](docs/PROMPT_AND_TASK_ROUTING.md) and [docs/AGENT_WORKFLOW.md](docs/AGENT_WORKFLOW.md) for binding rules.

## Active Work Package

- **Active work package:** None (Neutral post-merge state on `master`).
- **Current canonical baseline:** PR #156 is merged on `master` at commit `d8e14a699ef52030d542309015064d8ac0668508`.
- **Lifecycle status:** PR #156 (`fix: unify settings and onboarding visual consistency`) successfully completed its full multi-slice lifecycle: implementation checkpoints `1/5` through `5/5`, consolidated review, post-review correction (`3b07f4ffe5b07180ed303921a04fa4e557566285`), documentation candidate finalization (`571165cfd055ce9c5004c1d53f2ae6c6b005abb4`), exact-candidate `FULL_VALIDATION` (2541 tests passed, 0 warnings/errors), push, PR #156, manual user merge, and `POST_MERGE_SYNC_ONLY`. No repository-writing package is currently in flight.
- **Next planned work:** Milestone 19h (Home Personalization & Greeting) remains the next planned feature milestone on the Roadmap, pending explicit orchestration authorization.
- **Persistence boundary:** `DatabaseSchema.CurrentVersion` remains 12 and portable archive format remains V2.
- **Package provenance & live state:** Authoritative live checkout/branch, worktree state, and operational task positions are discovered directly from Git/GitHub, with `master` as the canonical branch.
- **Previous merged packages:**
  - PR #156 (`fix: unify settings and onboarding visual consistency`): Unified Settings and onboarding around shared KnownFirst visual primitives (`.choice-grid`, `.choice-button.active`, `.field-group`, `.text-input`, `.button-row`, `.setting-feedback`, `.destructive-confirmation`), aligned Daily Budget UX visual order (`5 Recommended`, `1`, `10`, `Custom`) and range/validation contracts, added onboarding System Language and Appearance selection in Step 1, and normalized inline destructive confirmation for Online Dictionary consent revocation. Merged to `d8e14a699ef52030d542309015064d8ac0668508`. `POST_MERGE_SYNC_ONLY` completed.
  - PR #155 (`feat: add first-run onboarding and daily budget ux`): Merged First-Run Onboarding & Daily-Budget UX Core package (`onboarding-core-v1`). Merged to `537e68eedf78dadb7b1ebff539954cd4d83228bf`. `POST_MERGE_SYNC_ONLY` completed.
  - PR #144 (`feat: add settings GUI and learning-day defaults`): Merged Settings GUI & Learning-Day Defaults Slice 2A. `POST_MERGE_SYNC_ONLY` completed.
  - PR #149 (`release: bump beta13 build number to 14`): Bumped version code to 14 for Google Play replacement bundle. Merged to `35cd15bf3e0dda0816bb2dd2c9dc35fa91ddd430`. `POST_MERGE_SYNC_ONLY` completed.
  - PR #152 (`docs: record build14 aab packaging evidence`): Reconciled durable release documentation with physical Google Play AAB bundle `KnownFirst-1.0.0-beta.13-code14.aab` produced and verified locally on certified `master` commit `8cd98d27ff81d8134b4e3b9d4b32b9b85abe3cb2`. Merged to `2a5b10735a1ea27e3db348e0cd855d7768376372`. `POST_MERGE_SYNC_ONLY` completed.
  - PR #153 (`feat: add onboarding install-origin foundation`): Merged First-Run Onboarding & Daily-Budget UX Slice 1 — application-local preference-backed onboarding state (`Required`/`InProgress`/`Completed`), startup install-origin classification via legacy preference evidence without database-file dependency, grandfathered legacy budget pinning (10), and destructive/non-destructive reset contracts. Merged to `aef5662cf4c4ad07ad937a35cdd15b3a793e4e59`. `POST_MERGE_SYNC_ONLY` completed.
  - PR #154 (`docs: simplify multi-slice governance`): Established the multi-slice feature-package lifecycle — checkpoint commits with `KnownFirst-Checkpoint:` trailers, consolidated package-level review and documentation gates, candidate finalization without artificial empty commits, the interruption/resume contract, and anti-treadmill documentation discipline — while preserving every repository, safety, and exact-HEAD validation invariant. Merged to `18c1e7998c1ffc34607e0a6feb54e930996353bd`. `POST_MERGE_SYNC_ONLY` completed.

## Verified Baseline & Release Boundaries

- **Canonical `master` baseline:** Live canonical `master` identity is discovered dynamically from Git/GitHub per [docs/NEW_CHAT_BOOTSTRAP.md](docs/NEW_CHAT_BOOTSTRAP.md). Prior PR merge commits and their `POST_MERGE_SYNC_ONLY` completions are immutable historical facts recorded in the merged-package history above.
- **Historical exact-master validation evidence:**
  - `ValidateAll-20260816-170329.log` on `f387f8a8...`: PASSED (1963/1963 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 AOT, 0 prohibited release diagnostics).
  - `ValidateAll-20260816-194413.log` on `eb4a2302...`: PASSED (1963/1963 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 AOT, 0 prohibited release diagnostics, 73/73 UI contract tests, 10/10 diagnostic gate tests).
  - `ValidateAll-20260816-234539.log` on `c9fc3fec...`: PASSED (1964/1964 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 Native AOT, 0 prohibited release diagnostics, strict compiler warning parity active with 0 warnings / 0 errors).
  - `ValidateAll-20260817-005330.log` on `e23c403...`: PASSED (1964/1964 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 Native AOT, 0 prohibited release diagnostics, strict compiler warning parity active with 0 warnings / 0 errors).
  - `ValidateAll-20260817-015455.log` on `38814434...`: PASSED (1964/1964 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 Native AOT, 0 prohibited release diagnostics, strict compiler warning parity active with 0 warnings / 0 errors).
  - `ValidateAll-20260817-025247.log` on candidate `e4cd038...` (PR #124): PASSED (1966/1966 automated tests, Windows Debug/Release, Android Debug/Release, 114/114 Native AOT, 0 prohibited release diagnostics, strict compiler warning parity active with 0 warnings / 0 errors, 20/20 Android publishing contract tests).
  - `ValidateAll-20260818-191908.log` on candidate `6b4ae35b3f57029dc799102e7c4e56741d60be82` (PR #131): PASSED (2039/2039 automated tests, Windows Debug/Release, Android Debug/Release, 0 failures, 0 skipped, exit code 0).
- **Historical packaging attempts:**
  - **Initial attempt on `eb4a2302...` (2026-08-16):** Canonical `GooglePlayBundle` execution failed (`GooglePlayBundle-20260816-195953.log`, exit code 1) because the publisher's `-warnaserror` promoted 8 XML documentation compiler warnings to errors. Resolved in PR #121.
  - **Second attempt on `38814434...` (2026-08-17):** Following Gate-17 certification on `38814434...`, canonical `GooglePlayBundle` execution failed (`GooglePlayBundle-20260817-021326.log`, exit code 1) before `dotnet publish` because stale `*-Signed.aab` output survived internal `dotnet clean` under `publish/`. Classified as `CANONICAL_PACKAGING_WORKFLOW_DEFECT` and resolved in PR #124.
- **Pre-AAB Gate Status:** On 2026-08-24, fresh exact-master technical validation (`ValidateAll-20260824-042536.log`), Gate 12 manual visual check ('visual test ok'), Gate 16 blocker evaluation, and Gate 17 Pre-AAB release-readiness certification were **COMPLETED AND CERTIFIED FOR GOOGLE PLAY INTERNAL TESTING** on synchronized `master` commit `8cd98d27ff81d8134b4e3b9d4b32b9b85abe3cb2`.
- **Source identity:** `1.0.0-beta.13` (build 14) merged on `master` via PR #149. Merging is not a packaging, signing, or distribution event.
- **Confirmed external distribution:** `1.0.0-beta.12` / build 12 (Google Play Internal Testing, confirmed 2026-07-30). No newer Android external distribution has occurred.
- **Database schema:** SQLite `PRAGMA user_version` 12 on `master` (Daily New-Word Limit & Learning-Day Infrastructure, merged via PR #142).
- **Supported platforms:** Android (Google Play Internal Testing) and Windows development/verification. iOS and Mac Catalyst remain removed.
- **Windows distribution packaging:** A real self-contained Windows Portable Release ZIP (`KnownFirst-1.0.0-beta.13-build13-win-x64-9e455d0.zip`) and matching SHA-256 sidecar were produced and verified on `master` at source commit `9e455d0e03494cac8e713cd4d16c66946124f852` on 2026-08-16. Clean-PC runtime execution, MSIX packaging, and external distribution remain separate unexecuted milestones.
- **Android Release APK:** A signed Android Release APK (`KnownFirst-1.0.0-beta.13-android-release.apk`, SHA-256 `53bbcb18b62927dae0af0a63d0e6a3cda6a8420c1c9517d354c638504b9ac6b6`) was produced and verified on physical hardware for manual Android validation.
- **Final Release AAB:** **COMPLETED / VERIFIED LOCALLY** (`KnownFirst-1.0.0-beta.13-code14.aab`, `48,002,097` bytes, SHA-256 `7a84da599ae7435614d95ff316707669d69e21b311fe252f5419ac9cb8ecbbcd`, `StrictVerified`, built from certified `master` commit `8cd98d27ff81d8134b4e3b9d4b32b9b85abe3cb2`).
- **Google Play Internal Testing Upload/Distribution:** **NOT YET PERFORMED / NOT AUTHORIZED BY CURRENT REQUEST** (Google Play Console upload for code14 requires separate explicit user authorization). Closed Testing, Open Testing, Production, and public rollout remain unauthorized.

## Daily New-Word Budget & Learning-Day Infrastructure — Slice 1 (Merged Production State)

Slice 1 is merged production `master` state via PR #142 (`feat: add daily new-word learning-day budget`; merge commit `34afed431711dd165b334d66b50b251a839faf02`; validated PR head `e7b6a0ad6a1159f94035b813bf747325bc314e8a`), on top of German Enhanced Term Recognition Package 5B (PR #140). Full architectural framing: [docs/PROJECT_STATE.md](PROJECT_STATE.md) "Daily New-Word Budget & Learning-Day Infrastructure — Slice 1 (Merged Production State)"; exact database and schema contracts: [docs/DATABASE_CONTRACT.md](DATABASE_CONTRACT.md) "Schema-12 Learning-Day and Daily New-Word Budget Contract."

- **Scope:** implements the non-visual daily new-word admission budget ($N \in \{5, 10, 20, 30, 50\}$, default 10) for distinct genuinely-new `WordId` values, durable Schema-12 `LearningDayState` singleton and `LearningDayGrants` persistence for ActiveBudgetDay/Bridge semantics, learning-timezone resolution (System/Explicit) and configurable learning-day cutoff (default 00:00) infrastructure, active-session rollover reconciliation, and V2 archive local exclusion. Excludes Settings UI, onboarding, install-origin classification, Restore Defaults UI, What's New, Display Name, ETR default/UI changes, and release packaging.
- **`IMPLEMENT` & Review:** initial implementation passed focused tests; correction cycle closed regression coverage gaps and fixed XML documentation warning; independent `REVIEW_ONLY` approved with `REVIEW_APPROVED_FOR_DOCUMENT_ONLY`.
- **Exact-head `FULL_VALIDATION` (validated PR head `e7b6a0ad6a1159f94035b813bf747325bc314e8a`):** 2276 passed / 0 failed / 0 skipped, Windows Debug PASS, Windows Release PASS, Android Debug PASS, Android Release PASS, strict warning/linking gate PASS with 0 warnings / 0 errors, exit code 0 (log `artifacts/launcher-logs/ValidateAll-20260823-004831.log`).
- **Evidence boundary (explicit):** this evidence proves only the automated scope the canonical launcher executed (2276 automated tests, Windows Debug/Release builds, Android Debug/Release builds). It does not prove GUI/manual UX correctness, device/emulator runtime behavior, APK/AAB installation behavior, signing correctness, or Google Play readiness/publication.
- **Remaining lifecycle:** none. `DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-candidate-HEAD `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`, manual user merge, and `POST_MERGE_SYNC_ONLY` are all complete for Slice 1. The feature branch `feature/daily-new-word-limit-learning-day-v1` was deliberately preserved after merge; its preservation is not a production-state fact and does not affect merged `master` behavior.

## Settings GUI & Learning-Day Defaults — Slice 2A (Merged Production State)

Slice 2A is merged production `master` state via PR #144 (`feat: add settings GUI and learning-day defaults`; merge commit `3c3b976b25a8e90da8c6f41ab8b9d667dead99cb`; validated PR head `40deec3be3b9672130804b42b1967922a07c1815`; merge parents `d3040b6a4aef0b9895b1aaad2cafc84315d70391` and `40deec3be3b9672130804b42b1967922a07c1815`), on top of Daily New-Word Limit & Learning-Day Infrastructure Slice 1 (PR #142). The initial candidate was committed as `0d0d5b9f52a7823cb793dea94ca319f48fae9ec7`; pre-PR user rendered UI feedback and technical analysis identified layout/ordering corrections, deterministic 24-hour cutoff presentation needs, and timezone edge coverage expansion, which were implemented, independently reviewed, committed as `40deec3be3b9672130804b42b1967922a07c1815`, validated, pushed, opened as PR #144, and merged.

- **Scope:** exposes Settings GUI controls for the Slice-1 learning-timezone and learning-day-cutoff infrastructure that Slice 1 left non-visual, adds a new non-destructive "Restore default settings" action distinct from the existing destructive "Reset all application data," and changes the Enhanced Term Recognition (ETR) missing-preference default from OFF to ON. Settings now renders in this exact product order: (1) Language, (2) Appearance, (3) New words per day, (4) Learning time zone, (5) New learning day begins at, (6) Card directions, (7) Learning mode, (8) Enhanced Term Recognition, (9) Online dictionary lookup, (10) Portable data, (11) Help & Support, (12) Restore default settings, (13) Reset all application data. Option presentation orders place default choices leftmost: Card directions renders (1) Both directions (`Beide Richtungen`), (2) Term -> Meaning, (3) Meaning -> Term; Learning mode renders (1) Automatic (`Automatisch`), (2) Reading, (3) Typing. Learning timezone status (`#learning-timezone-effective`) includes positive vertical separation (`.setting-status` with `margin: var(--space-3) 0 0`) below the timezone selector to prevent visual collision with the dropdown boundary. Debug-only diagnostics remain separately gated (`#if DEBUG || KNOWNFIRST_DIAGNOSTICS`) and are not Release product surface. New words per day continues to reuse the existing `PreparationLimit`/`PreparationLimitPolicy` contract ($N \in \{5, 10, 20, 30, 50\}$, default 10); no parallel limit policy was introduced.
- **Target defaults:** Language = System; Appearance = System; New words per day = 10; Learning time zone = System; New learning day begins at = 00:00; Card directions = Both; Learning mode = Automatic; Enhanced Term Recognition = On.
- **Learning time zone:** default System, meaning `TimeZoneInfo.Local` (OS timezone) only — no GPS, IP geolocation, location permission, account, cloud, or network dependency. Explicit selection uses a bounded, curated catalog (`KnownFirst.Core.Settings.LearningTimezoneCatalog`) of 50 canonical IANA timezone identifiers (spanning inhabited UTC-11 through UTC+14, including `Pacific/Pago_Pago`, `Pacific/Chatham`, `Pacific/Tongatapu`, and `Pacific/Kiritimati`); the catalog stores only the canonical ID, never a UTC offset. Settings option labels have the user-facing form `(UTC±HH:mm) City`, with the offset computed at render time from `TimeZoneInfo`/`DateTime.UtcNow` (dynamic, DST-aware, including Chatham UTC+12:45 standard / UTC+13:45 daylight) rather than persisted. An invalid persisted timezone ID still fails safely to System behavior through the existing, unmodified `LearningTimezoneResolver`. EN/DE/RU localization is complete (50/50 catalog city names in each language). **Evidence boundary:** Windows/.NET runtime resolution of all 50 catalog IDs was verified (via `Catalog_EveryCuratedIdResolvesOnTheCurrentPlatform` and `Catalog_CoversInhabitedEdgeTimezones`). Android runtime resolution, emulator/device validation, and Android timezone-runtime proof were **not** performed and remain unverified; the later mandatory `FULL_VALIDATION` Android Debug/Release **build** gates are not equivalent to Android runtime/device proof.
- **New learning day begins at:** default 00:00; deterministic two-part 24-hour selector (`#learning-day-cutoff-hour` covering `00`..`23`, `#learning-day-cutoff-minute` covering `00`..`59`, separated by visual colon `.time-separator`) with localized accessible labels (`Settings_LearningDayCutoffHours`, `Settings_LearningDayCutoffMinutes`), replacing native `<input type="time">` without AM/PM or host regional format dependencies; the valid stored domain remains 0..1439 minutes, unchanged in `LearningDayConfiguration.NormalizeCutoffMinutes`; arbitrary values such as 07:37 are valid; `24:00` is impossible. The value is interpreted using the existing `LearningDayConfiguration`/`LearningDayBoundaryPolicy` and the selected learning timezone; no duplicate cutoff policy exists. Existing Schema-12 `ActiveBudgetDay`/Bridge/rollover semantics are unchanged — `Services/Study/LearningService.cs`, `LearningDayBoundaryPolicy`, and `Data/` show no diff in this candidate.
- **Restore default settings vs. Reset all application data:** these are two distinct contracts, deliberately kept separate.
  - **Restore default settings** is non-destructive: it restores the target defaults above (settings, theme preference, language preference) and **preserves the user's current online-dictionary lookup consent exactly as it is** — granted stays granted, not granted stays not granted. It does not call `Database.ResetAsync()`, does not call `Preferences.Clear()`, and does not touch vocabulary, learning cards, review/learning history, imported documents, other database content, Display Name, onboarding state, What's-New/version state, or any other unrelated persisted product state.
  - **Reset all application data** remains the separate destructive action, remains last in the Settings product order, and retains its existing destructive confirmation, `Database.ResetAsync()` call, and `Preferences.Clear()` call. After a full reset, online-dictionary lookup consent is always false/not granted (unconditionally revoked, independent of whatever the in-memory consent value was immediately before `Preferences.Clear()`), and ordinary settings defaults are restored afterward via the same shared default-restoration policy (`Services.Settings.SettingsDefaultsService`, exposing `RestoreDefaults()` for the non-destructive path and `RestoreDefaultsForFullReset()` for the destructive path, so both flows cannot drift apart on the target defaults while keeping the consent contracts distinct).
- **Enhanced Term Recognition default transition:** the authoritative default for a missing `enhanced_term_recognition_enabled` preference is now ON (`KnownFirst.Core.Settings.EnhancedTermRecognitionPolicy.DefaultEnabled = true`, the single source of truth). An explicitly persisted `false` remains OFF. Both "Restore default settings" and "Reset all application data" restore ETR to ON. Existing German ETR runtime behavior (`TextReviewService`, `ConservativeGermanCompoundDecomposer`, Schema-11 `DerivedTermEvidence` persistence) is otherwise unchanged. **Upgrade implication (explicitly accepted for this candidate, not a defect):** an existing installation that has no persisted `enhanced_term_recognition_enabled` key — including an installation that has never touched the toggle — will resolve to ON after upgrading to a build containing this change, since the physical preference store makes no distinction between "fresh install" and "pre-existing install that never set the key." No install-origin migration infrastructure was added or is planned for this package.
- **Schema/archive:** `DatabaseSchema.CurrentVersion` remains 12; portable archive format remains V2. No schema, migration, or archive contract changed.
- **Lifecycle evidence:**
  - Initial Slice-2A candidate committed as `0d0d5b9f52a7823cb793dea94ca319f48fae9ec7` passed an earlier `FULL_VALIDATION` (`artifacts/launcher-logs/ValidateAll-20260823-014311.log`), which became stale once pre-PR UI corrections and catalog expansion modified repository files afterward.
  - Pre-PR UI corrections & deterministic 24-hour cutoff / 50-zone catalog:
    - Layout and ordering corrections (timezone status spacing, default-first option ordering, Restore Defaults directly above Reset all application data).
    - Deterministic 24-hour cutoff selector (`#learning-day-cutoff-hour`, `#learning-day-cutoff-minute`, `.time-separator`) and 50-entry timezone catalog expansion (`Pacific/Pago_Pago`, `Pacific/Chatham`, `Pacific/Tongatapu`, `Pacific/Kiritimati`).
    - Focused TDD: genuine RED (17 passed / 4 failed) → focused GREEN (164 passed / 0 failed / 0 skipped across catalog, UI contracts, localization, cutoff precision, settings defaults, app settings, bug report launcher).
    - Focused Windows Debug build succeeded with 2 MSB3270 warnings (WindowsAppSDK projection assembly architecture mismatch) and 0 errors; this focused-build evidence is historical context only and is not the authoritative pre-PR gate.
    - Independent `REVIEW_ONLY`: 0 BLOCKER / 0 MAJOR / 0 MINOR / 0 NIT; decision `REVIEW_APPROVED_FOR_DOCUMENT_ONLY`.
  - `DOCUMENT_ONLY` reconciliation, `COMMIT_ONLY` (candidate HEAD `40deec3be3b9672130804b42b1967922a07c1815`), and exact-candidate-HEAD `FULL_VALIDATION` all completed: 2339 passed / 0 failed / 0 skipped, Windows Debug PASS, Windows Release PASS, Android Debug PASS, Android Release strict PASS, exit code 0 (log `artifacts/launcher-logs/ValidateAll-20260823-044728.log`); validation marker `FULL_VALIDATION_PASSED_EXACT_HEAD_40deec3be3b9672130804b42b1967922a07c1815`.
  - **Evidence boundary (explicit):** this evidence proves only the automated scope the canonical launcher executed (2339 automated tests, Windows Debug/Release builds, Android Debug/Release strict builds). It does not prove rendered/manual post-edit Settings GUI correctness, Android device/emulator runtime behavior, Android runtime resolution of every timezone ID, APK/AAB/MSIX creation or installation, Google Play readiness, Onboarding completion, Install-Origin/What's-New completion, Windows Store completion, Web/PWA completion, or public-release readiness.
  - `PUSH_ONLY`, `PR_ONLY` (PR #144, `feat: add settings GUI and learning-day defaults`), manual user merge (merge commit `3c3b976b25a8e90da8c6f41ab8b9d667dead99cb`), and `POST_MERGE_SYNC_ONLY` all completed; local `master`/`origin/master` synchronized to `3c3b976b25a8e90da8c6f41ab8b9d667dead99cb`.
- **Remaining lifecycle:** none. `DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-candidate-HEAD `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`, manual user merge, and `POST_MERGE_SYNC_ONLY` are all complete for Slice 2A. The feature branch `feature/settings-gui-learning-day-defaults-v1` was preserved after merge; its preservation is not a production-state fact and does not affect merged `master` behavior.

## German Enhanced Term Recognition — Package 5A (Merged Production State)

Packages 1–4 (PR #134) and Package 5A (PR #135) are both merged production `master` state and are documented as stable capabilities in [docs/PROJECT_STATE.md](PROJECT_STATE.md). Package 5A — derived-term post-review lifecycle integrity — is merged to `master` at `683f34473dd21417be9d8e1b60d04de539fb35a8`. Full architectural framing: [docs/PROJECT_STATE.md](PROJECT_STATE.md) "German Enhanced Term Recognition — Package 5A (Merged Production State)."

- **Scope:** corrects the lifecycle of a derived compound component's `ReviewCandidateEntity`/`DerivedTermEvidenceEntity` around and after review completion, given that a derived candidate deliberately never receives a `WordOccurrenceEntity`. Touches `TextReviewService.CompleteSession`, `DocumentCleanupOperations`, `PreparationService`/`PreparationServiceSchema8`/`Schema8EvidenceScanner`, and the portable V2 backup mapper (`BackupModelMapperV2`, `Schema8BackupSnapshotRepository`, `Schema8BackupSnapshot`, `BackupSnapshotCapture`). No schema/version change, no archive-format change.
- **Unknown derived evidence retention:** when a derived candidate is decided `UnknownBacklog`, its owning `ReviewCandidateEntity` and `DerivedTermEvidenceEntity` row(s) now survive normal review-session completion, so Preparation can still recover a real source-compound context. Known/Ignored derived candidates and every Direct candidate continue to be cleaned up exactly as before.
- **Sentence/document dependency protection:** the `Document` and exact `SentenceSpan` a surviving evidence row depends on are now protected from generic maintenance cleanup (document/sentence-span sweeps reached from any unrelated review or preparation completion elsewhere in the app), scoped through the real candidate→session→document/sentence foreign-key relationships. Genuinely unreferenced documents/sentences with no retained evidence remain fully cleanup-eligible.
- **Preparation source-compound context fallback:** both the review-words display path and the Accept path now build context from the retained evidence's real whole-compound source span (never a fabricated component occurrence) when zero valid ordinary occurrence contexts remain, and agree with each other on when that fallback activates.
- **Cleanup on MarkKnown/Exclude:** when the retaining word leaves the Unknown lifecycle, its retained evidence and owning candidate are deleted at that point, scoped to exactly that word.
- **Portable V2 filter (superseded by Package 5A-2, see below):** on merged Package 5A, the portable/full-backup snapshot capture identified which captured `ReviewCandidates` own retained derivation evidence (Schema-11-only, internal enrichment, never itself exported) and the V2 mapper excluded exactly those specific candidate rows from exported review-workflow items, so a Completed session would not leak a provenance-less item. This exclusion is superseded by merged Package 5A-2 (PR #137): the retained candidate now exports through the ordinary completed-review-item path with its evidence transported alongside it — see "German Enhanced Term Recognition — Package 5A-2" below.
- **Evidence boundary:** the exact-head `FULL_VALIDATION` above proves only the automated scope the canonical launcher executed on the merged Package-5A candidate — 2227 automated tests, Windows Debug/Release builds, Android Debug/Release builds. It does not prove GUI/manual UX correctness, device/emulator runtime behavior, APK/AAB installation behavior, signing correctness, Google Play readiness/publication, or evidence beyond what is separately recorded below for Package 5A-2.

### Review and evidence history (historical — Package 5A is now merged)

- **`IMPLEMENT`:** completed.
- **First independent `REVIEW_ONLY`:** found 1 BLOCKER (generic document cleanup could orphan retained Schema-11 derived evidence), 1 MAJOR (retained Completed-session `ReviewCandidates` could accidentally alter portable V2 export semantics), 1 MINOR (Preparation display/Accept derived-evidence fallback used inconsistent gating).
- **Correction:** all three findings corrected; correction-focused verification reported 481 passed / 0 failed / 0 skipped, with clean `git diff --check`.
- **Final independent `REVIEW_ONLY`:** 0 BLOCKER / 0 MAJOR / 0 MINOR, approved for `DOCUMENT_ONLY`. Targeted verification re-run during that review: 141/141 passed (`GermanDerivedTermPreparationTests`, `TextReviewServiceTests`, `BackupCreationTests`, `Schema10CapabilityActivationTests`, `Schema11CapabilityActivationTests`) and 49/49 passed (`PreparationServiceSchema8*`); `git diff --check` clean. Three non-blocking NITs were recorded (two latent schema-gate consistency observations in Preparation code with no currently reachable failure path, one imprecise `BackupSnapshotCapture` documentation comment); see [docs/BACKLOG.md](BACKLOG.md) for the two code-quality NITs.
- **`DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-head `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`:** all completed. Commit `5d4efa48b0368ef3a68d47963c7643c0ca57b57b`; exact-head `FULL_VALIDATION` 2227 passed / 0 failed / 0 skipped, Windows Debug/Release PASS, Android Debug/Release PASS, exit code 0 (log `artifacts/launcher-logs/ValidateAll-20260821-001937.log`); PR #135 opened and merged to `683f34473dd21417be9d8e1b60d04de539fb35a8`.
- **`POST_MERGE_SYNC_ONLY`:** completed successfully; local `master`/`origin/master` synchronized to `683f34473dd21417be9d8e1b60d04de539fb35a8`. The subsequent documentation-closure package (PR #136) is also merged; `master` HEAD is `4d5038d6292ee274b9b38b04fe5c582f617fd853`.

## German Enhanced Term Recognition — Package 5A-2 (Merged Production State)

Package 5A-2 is merged production `master` state via PR #137 (`feat: transport German derived evidence portably`; merge commit `5d1d3c05bae6ab9f1c56d8c5f9a227121f432f9a`; validated PR head `2ff447e9f874d49e72fee0a549820adc1bdc3b39`), on top of Package 5A (PR #135). Full architectural framing: [docs/PROJECT_STATE.md](PROJECT_STATE.md) "German Enhanced Term Recognition — Package 5A-2 (Merged Production State)"; exact archive/schema/merge contract: [docs/DATABASE_CONTRACT.md](DATABASE_CONTRACT.md) "Schema-11 Derived-Term Evidence Contract."

- **Scope:** implements the cross-installation transport of retained `DerivedTermEvidenceEntity` rows that Package 5A intentionally left unserialized — through ordinary portable export, full/internal backup, merge safety copy, empty-target restore, and populated-target merge. No schema/version change (`DatabaseSchema.CurrentVersion` remains 11), no archive-format change (archive stays V2).
- **`IMPLEMENT`:** completed. Focused implementation-phase test scopes passed as reported by the `IMPLEMENT` phase.
- **Correction (acceptance-coverage hardening):** an independent `REVIEW_ONLY` found no production-code defect and two MAJOR test-coverage gaps (Exclude cleanup for imported/merged evidence; Document/SentenceSpan generic-cleanup protection for imported/merged evidence). Both were closed by adding `PopulatedTargetMerge_ExcludeCleansUpMergedEvidence` and `PopulatedTargetMerge_GenericDocumentCleanupDoesNotOrphanMergedEvidence` to `KnownFirst.Tests/GermanDerivedTermEvidenceMergeTests.cs`; both passed immediately as characterization/hardening evidence (1 passed / 0 failed each), with no production code change. A combined focused scope of exactly those two new tests plus the existing imported-evidence MarkKnown test and the existing native-evidence cleanup-protection test passed 4 passed / 0 failed.
- **Final independent `REVIEW_ONLY`:** 0 BLOCKER / 0 MAJOR findings; `Ready for DOCUMENT_ONLY: YES`. Three MINOR findings were recorded and intentionally deferred as non-blocking: (1) the repeated-import convergence test proves no duplicate physical rows but does not assert plan-classification-level `ExactDuplicateSkipped` for `DerivedTermEvidence`; (2) unrelated-candidate regression coverage checks word status and candidate row count rather than a full field-by-field `ReviewCandidateEntity` comparison; (3) a cosmetic indentation inconsistency in `KnownFirst.Tests/MergePreflightPlannerTests.cs`.
- **Exact-head `FULL_VALIDATION`:** 2248 passed / 0 failed; Windows Debug PASS; Windows Release PASS; Android Debug PASS; Android Release PASS; exit code 0; log `artifacts/launcher-logs/ValidateAll-20260821-233217.log`. `POST_MERGE_SYNC_ONLY` completed successfully; local `master`/`origin/master` synchronized to `5d1d3c05bae6ab9f1c56d8c5f9a227121f432f9a`.
- **Evidence boundary (explicit):** this evidence proves only the automated scope the canonical launcher executed (2248 automated tests, Windows Debug/Release builds, Android Debug/Release builds). It does not prove GUI/manual UX correctness, device/emulator runtime behavior, APK/AAB installation behavior, signing correctness, or Google Play readiness/publication.
- **Remaining lifecycle:** none. `DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-candidate-HEAD `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`, manual user merge, and `POST_MERGE_SYNC_ONLY` are all complete for Package 5A-2. The feature branch `feature/german-package-5a-2-portable-derived-evidence-transport-v1` was deliberately preserved after merge; its preservation is not a production-state fact and does not affect merged `master` behavior. The subsequent documentation-closure package (PR #138, `docs: close german package 5a-2 post-merge`) is also merged, merge commit `4963f1745235e3ad745bce8689f47db52b62f98e`; its `POST_MERGE_SYNC_ONLY` completed successfully.

## German Enhanced Term Recognition — Package 5B (Merged Production State)

Package 5B is merged production `master` state via PR #140 (`feat: show German derivation source in review`; merge commit `bd67393f81cece98c3d8c58c5ea26ef3e8920079`; validated PR head `d1de7556bfd346f3aa1fdd19741c7d1a6d647ba6`), on top of Package 5A-2 above. Full architectural framing: [docs/PROJECT_STATE.md](PROJECT_STATE.md) "German Enhanced Term Recognition — Package 5B (Merged Production State)."

- **Scope:** adds a minimal, always-visible Review Words derivation-source indication for genuine `CandidateProvenanceKind.DerivedFromCompound` candidates, consuming the already-existing `ReviewCandidateDetails.Provenance`/`DerivationEvidence` projection that `TextReviewService.GetCurrentCandidateAsync()` already populated; no new service/query/database boundary was introduced. Also adds native (non-merge) Exclude cleanup characterization and derived Preparation→Learning continuity characterization, closing the remaining approved German derived-term regression-coverage gaps identified in [docs/ROADMAP.md](ROADMAP.md) Priority 19c. No schema/version change (`DatabaseSchema.CurrentVersion` remains 11), no archive-format change (archive stays V2).
- **Displayed source:** the notice uses `DerivedTermEvidence.SourceSurfaceForm` only (deduplicated, blank-filtered, deterministically joined for multiple distinct source compounds); it never displays `SourceIdentity`, database/archive ids, positions, hashes, or `ComponentForm`.
- **Placement:** the notice renders immediately after the candidate heading and before the collapsed metadata details panel, so it is visible without expanding that panel; it is normal production UI, not Debug-gated.
- **Unaffected behavior:** Direct candidates are visually unchanged; Review Words `Contexts` semantics are unchanged (no sentence-context reconstruction was added); Package 5A's Preparation retained-evidence context-recovery fallback remains the post-review-completion mechanism unchanged; MarkKnown/Exclude cleanup, Known-suppression, and Document/SentenceSpan protection semantics are all unchanged.
- **`IMPLEMENT`:** completed. Stage-A genuine TDD: `ReviewWords_DerivedCandidateShowsSourceCompoundNoticeOutsideCollapsedDetails` RED 0 passed / 1 failed (missing derived-source Review Words markup) → GREEN 1 passed / 0 failed. Characterization/hardening: `CompletingPreparationWithoutLearning_ExcludeRemovesRetainedDerivationEvidence` and `DerivedUnknownAccept_EntersNormalLearningPathWithoutDuplicateIdentity`, both passed immediately (2 passed / 0 failed), protecting already-correct production behavior with no production correction required.
- **Focused affected-scope verification:** `UiWorkflowContractTests|GermanDerivedTermPreparationTests|TextReviewServiceTests|LocalizationResourceTests` — 163 passed / 0 failed. `git diff --check` PASS.
- **Independent `REVIEW_ONLY`:** 0 BLOCKER / 0 MAJOR; two non-blocking MINOR findings (a source-level styling observation that very long German source compounds could benefit from explicit wrapping/hyphenation on narrow viewports, and a singular-wording observation when multiple distinct source compounds are joined) and one NIT (negligible per-render recomputation); decision `READY_FOR_DOCUMENT_ONLY`. These are recorded operational review-history observations, not confirmed rendered-GUI defects or established product limitations.
- **Exact-head `FULL_VALIDATION` (validated PR head `d1de7556bfd346f3aa1fdd19741c7d1a6d647ba6`):** 2251 passed / 0 failed; Windows Debug PASS; Windows Release PASS; Android Debug PASS; Android Release PASS; strict warning/linking gate PASS; exit code 0; log `artifacts/launcher-logs/ValidateAll-20260822-142947.log`.
- **Evidence boundary (explicit):** the `IMPLEMENT`/`REVIEW_ONLY` evidence above is source/markup-contract and automated service/integration-characterization evidence only, using isolated temporary SQLite databases. The exact-head `FULL_VALIDATION` evidence additionally proves the complete automated test suite and Windows/Android Debug/Release build success for that exact commit. Neither proves rendered GUI/WebView correctness, device/emulator runtime behavior, APK/AAB installation behavior, signing correctness, or Google Play/store readiness.
- **Remaining lifecycle:** none. `DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-candidate-HEAD `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`, manual user merge, and `POST_MERGE_SYNC_ONLY` are all complete for Package 5B. `POST_MERGE_SYNC_ONLY` completed successfully; local `master`/`origin/master` synchronized to `bd67393f81cece98c3d8c58c5ea26ef3e8920079`.

## First-Run Onboarding + Daily New-Word Budget UX — Slice 1 (Merged Production State)

Slice 1 is merged production `master` state via PR #153 (`feat: add onboarding install-origin foundation`; merge commit `aef5662cf4c4ad07ad937a35cdd15b3a793e4e59`; validated PR head `36534afa4664eea99fcb41b2554b72e64d7a35ec`), on top of Settings GUI Slice 2A (PR #144). Exact-head `FULL_VALIDATION` passed with 2364 passed / 0 failed / 0 skipped across `KnownFirst.Tests`, Windows Debug/Release PASS, Android Debug/Release PASS (0 warnings / 0 errors). `POST_MERGE_SYNC_ONLY` completed successfully; `DatabaseSchema.CurrentVersion` remains 12 and portable archive format remains V2.

- **Scope & Foundation:**
  - Implements application-local preference-backed onboarding state (`Required = 1`, `InProgress = 2`, `Completed = 3`) via `KnownFirst.Core.Settings.OnboardingState` and `OnboardingStatePolicy`.
  - Implements install-origin classification (`IInstallOriginClassifier` / `InstallOriginClassifier` registered as singleton) executing during startup in `MauiProgram.cs` before `ILanguageSelectionService.Initialize()` so language initialization markers do not manufacture false legacy evidence.
  - Distinguishes fresh installations (no onboarding marker + no legacy preference evidence => `Required`) from existing installations (no onboarding marker + legacy preference evidence => `Completed`). Database-file existence is deliberately not used as install-origin evidence. Already valid onboarding state is respected without reclassification.
  - Grandfathers existing installations with no persisted `preparation_limit` by pinning the legacy effective value `10` to `preparation_limit`. Existing explicit values are preserved; genuine fresh installs are not pinned. `PreparationLimitPolicy.DefaultLimit` remains 10 with existing presets ($N \in \{5, 10, 20, 30, 50\}$).
  - Reset contracts: Destructive full reset sets `OnboardingState.Required` first before default restoration recreates legacy markers; online dictionary consent remains unconditionally revoked. Non-destructive Restore Defaults leaves onboarding state untouched and preserves current online dictionary consent.
  - Persistence boundary: onboarding state is Preferences/application-local state, not SQLite. `DatabaseSchema.CurrentVersion` remains 12 and archive format remains V2. No SQLite tables, columns, migrations, or archive DTO changes.
- **Explicit Non-Goals / Later Slices:**
  - Visible first-run onboarding UI, `Routes.razor` routing/gating, resume-step UI, Display Name, Home personalization, new daily-budget presets `1 / 5 Recommended / 10 / Custom`, default change from 10 to 5, >15 custom-budget warning, onboarding localization, GUI-test-profile onboarding suppression, and What's New install-origin integration are not implemented in Slice 1 and remain planned for later slices.
- **Remaining lifecycle:** none. `DOCUMENT_ONLY`, `COMMIT_ONLY`, exact-candidate-HEAD `FULL_VALIDATION`, `PUSH_ONLY`, `PR_ONLY`, manual user merge, and `POST_MERGE_SYNC_ONLY` are all complete for Slice 1.

## Governed Operational Sequence & Next Actions

### First-Run Onboarding & Daily-Budget UX sequence

1. ~~**Install-Origin Foundation & Reset Contracts.**~~ Completed — merged via PR #153 (merge commit `aef5662cf4c4ad07ad937a35cdd15b3a793e4e59`).
2. **First-Run Onboarding + Daily New-Word Budget UX Core (active package `onboarding-core-v1`).** Six declared slices and post-review correction implemented and reviewed on `feature/onboarding-daily-budget-ux-core-v1`.
3. **Home greeting/personalization.** Planned as a separate later package after the onboarding core package closes.

### German Enhanced Term Recognition sequence — complete through Package 5B

1. ~~Packages 1–4, `COMMIT_ONLY`/`FULL_VALIDATION`/`PUSH_ONLY`/`PR_ONLY`/merge.~~ Completed — merged via PR #134 (merge commit `6c7a89ed6b4b0fc7701fdca8ec85a38b91bbeeb5`).
2. ~~Package 5A `IMPLEMENT` → focused verification → `REVIEW_ONLY` (1 BLOCKER/1 MAJOR/1 MINOR, corrected) → final `REVIEW_ONLY` (0/0/0) → `DOCUMENT_ONLY` → `COMMIT_ONLY` → exact-head `FULL_VALIDATION` → `PUSH_ONLY` → `PR_ONLY`.~~ Completed — merged via PR #135 (merge commit `683f34473dd21417be9d8e1b60d04de539fb35a8`).
3. ~~`POST_MERGE_SYNC_ONLY` for PR #135, and the subsequent documentation-closure package.~~ Completed — local `master`/`origin/master` synchronized to `683f34473dd21417be9d8e1b60d04de539fb35a8`, then to `4d5038d6292ee274b9b38b04fe5c582f617fd853` (PR #136).
4. ~~Package 5A-2 `IMPLEMENT` → focused verification → acceptance-coverage correction → final `REVIEW_ONLY` (0 BLOCKER/0 MAJOR) → `DOCUMENT_ONLY` → `COMMIT_ONLY` → exact-head `FULL_VALIDATION` → `PUSH_ONLY` → `PR_ONLY`.~~ Completed — merged via PR #137 (merge commit `5d1d3c05bae6ab9f1c56d8c5f9a227121f432f9a`).
5. ~~`POST_MERGE_SYNC_ONLY` for PR #137.~~ Completed — local `master`/`origin/master` synchronized to `5d1d3c05bae6ab9f1c56d8c5f9a227121f432f9a`.
6. ~~`DOCUMENT_ONLY` phase reconciling durable documentation with the completed Package 5A-2 merge, `COMMIT_ONLY`/exact-head `FULL_VALIDATION`/`PUSH_ONLY`/`PR_ONLY`/manual user merge.~~ Completed — merged via PR #138 (merge commit `4963f1745235e3ad745bce8689f47db52b62f98e`).
7. ~~`POST_MERGE_SYNC_ONLY` for PR #138.~~ Completed — local `master`/`origin/master` synchronized to `4963f1745235e3ad745bce8689f47db52b62f98e`).
8. ~~Package 5B `IMPLEMENT` → focused verification → independent `REVIEW_ONLY` (0 BLOCKER/0 MAJOR/2 non-blocking MINOR/1 NIT, `READY_FOR_DOCUMENT_ONLY`) → `DOCUMENT_ONLY` → `COMMIT_ONLY` → exact-head `FULL_VALIDATION` → `PUSH_ONLY` → `PR_ONLY`.~~ Completed — merged via PR #140 (merge commit `bd67393f81cece98c3d8c58c5ea26ef3e8920079`).
9. ~~`POST_MERGE_SYNC_ONLY` for PR #140.~~ Completed — local `master`/`origin/master` synchronized to `bd67393f81cece98c3d8c58c5ea26ef3e8920079`.
10. English multi-word recognition (`KF-LEX-003`) remains deferred and is not part of this German package sequence. No later German implementation package has started.

### Completed Historical Packaging — Beta-13 Build-14 AAB

Pre-AAB Gate certification and local signed bundle creation for `KnownFirst-1.0.0-beta.13-code14.aab` passed on `master` commit `8cd98d27ff81d8134b4e3b9d4b32b9b85abe3cb2`; packaging evidence was recorded and merged via PR #152. Google Play Console upload and Internal Testing distribution remain separately deferred until explicitly authorized.
