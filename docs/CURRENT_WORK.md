# KnownFirst Current Work

## Last updated

2026-07-29

## Repository

- Repository: https://github.com/Tachiguro/KnownFirst.git
- Only local project folder: C:\Dev\KnownFirst (see [ADR-0007](decisions/ADR-0007-single-canonical-working-directory.md))
- Active rule: use the single folder; create no worktree without explicit user approval.
- Only one writing agent may operate at a time.

## Stable baseline

- Stable master baseline: `7076aa4ac0617bd55ff97821ea69dc6b0e1228b0` (PR #22 merge commit)
- App version on master: `1.0.0-beta.11` (build 11) — master already contains Russian UI/translation-target support (PR #20), the learning repeat/direction clarity fix (PR #21), and the Beta 11 identity bump and What's New content (PR #22)
- Database schema: SQLite `PRAGMA user_version` 7 (unchanged)
- Supported platforms: Android (Google Play Internal Testing) and Windows development/verification. iOS and Mac Catalyst remain removed.
- Solution: KnownFirst.slnx

## Current branch

- Branch: `feature/windows-gui-test-launcher-v1`
- Base: originally `b5e4b055ed1ac626f237f0560bfa894e2fdc4e86` (PR #32 merge commit); reconciled with master at `b405aa49001538ac60f45dc9697d9308e48e9eb2` (PR #33 merge commit) via a local merge commit.
- **Current task:** Windows GUI-test launcher foundation — the `-Action GuiTest` launcher entry point, the StartupSmoke scenario runner, structured artifact reporting, and disposable GUI-test profile isolation. Committed locally as `2cbfa7f3221de45a7b2cd8cbd021742068180b09`, now merged with and reconciled against master.
- KF-MEANING-001 Slice 3 (multi-Sense preparation and topic persistence) is merged into master via [PR #33](https://github.com/Tachiguro/KnownFirst/pull/33). See "Design work completed but not yet implemented" below for its full history.
- Schema 8 remains dormant: `DatabaseSchema.CurrentVersion` remains `7`.

## Completed on current branch (this session)

**KF-MEANING-001 Slice 3 — multi-Sense preparation and topic persistence, dual-schema compatible**

- Branch: `feature/meaning-centric-multi-sense-preparation-v1`, base `b5e4b055ed1ac626f237f0560bfa894e2fdc4e86`. Committed as `a51b0e82e35b2bb82d707580fe4fa04f8ac79765`; pushed; PR #33 open (not merged).
- Implemented the §4.8.2 Slice 3 architecture contract (schema-capability-neutral preparation layer for Schema 8).
- Added preparation-specific schema-capability resolver (`PreparationSchemaCapability.cs`), independent of backup's `BackupSchemaCapability`.
- Real Schema-8 `StartAsync` dispatch: words with `PreparationState.Prepared` and existing Senses are eligible again when `Schema8EvidenceScanner` finds genuinely-new evidence (unbounded scan, matched by four-field key: `SourceDocumentId`, `NormalizedFingerprint`, `TargetStart`, `TargetLength`).
- Frozen candidate evidence at creation time, re-matched against live occurrence data at item creation (deterministic, no fresh scan). Schema-7 legacy selection/creation database-write behavior is unchanged; the code itself was refactored (extracted `AcceptSchema7`, delegated context/fingerprint helpers, an added validate-only metadata check on two brand-new optional fields) rather than left byte-for-byte identical — see [kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md) §D item 3 for the exact boundary.
- Versioned `PreparationCandidatePayloadV1` envelope with discriminated codec (`Empty`/`EnvelopeV1`/`LegacyLexicalResult`/`UnsupportedEnvelopeVersion`/`Malformed`).
- Lazy envelope upgrade (`EnsureCandidateEnvelopeAndSelection`): genuine envelopes never rewritten; Empty/Legacy upgraded on demand when accessed, with frozen evidence appended and Schema-8 shape verified before any transaction.
- Effective-processed-evidence ledger (`Schema8EvidenceLedger`): tracks `legacyBaselineKeys ∪ fullyResolvedPreparedEnvelopeKeys` (Empty/LegacyLexicalResult candidates own no evidence; partially-resolved/Pending/Failed/Skipped/Cancelled envelopes mark nothing as processed).
- Multi-Sense classification and deterministic all-exact auto-linking: provider indices matching existing Sense vocabulary (via `SemanticMeaningIdentityPolicy`) are auto-linked post-explicit-accept; `LookupCurrentAsync` opportunistically auto-resolves all matching indices and auto-completes candidates when all indices resolve.
- Provider-index integrity: `AcceptSchema8` revalidates index freshly inside transaction, rejects out-of-range/already-resolved/stale selections.
- Metadata persistence: `TopicOrDomain` and `PartOfSpeech` (Unicode Form C, trimmed, limits enforced) validated before mutation in both Schema-7 and Schema-8 paths; PartOfSpeech resolved from input, selected Meaning, or empty.
- Direction-specific preferred Meaning: each card (`LearningCardEntity`) has its own `PreferredMeaningId`; `SenseEntity.DefaultMeaningId` is a non-authoritative fallback only.
- Nine named fault-injection checkpoints: `AfterEnvelopePersist`, `AfterSenseInsert`, `AfterMeaningInsert`, `AfterContextLink`, `AfterCardInsert`, `AfterResolvedIndexPersist`, `DuringAutoExactVariantLinking`, `BeforeCandidateCompletion`, `BeforeAutomaticCandidateCompletion` — each proven to roll back completely.
- **Database schema unchanged** (`CurrentVersion` remains `7`, `InitializeAsync` unchanged, `Data/Entities` unchanged, migration dormant).
- **26 files changed** (5 modified, 21 added — regenerated from `git diff master...HEAD --name-status`): 1 new core-library policy file, 10 new service files, 9 new test files, plus the added handoff document itself.
- **Tests: 1190/1190 passed** (focused preparation 184/184, directly affected 230/230, complete suite) — last run recorded before the correction pass; see PR #33 for the current validation evidence.
- **No production code regressions**: `git diff master...HEAD --check` (the committed-range check) is clean after the correction pass.
- Committed as `a51b0e82e35b2bb82d707580fe4fa04f8ac79765`, pushed, and under review in [PR #33](https://github.com/Tachiguro/KnownFirst/pull/33). See handoff: [docs/handoffs/kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md).

## Completed on master (prior branches, already merged)

- Russian application-interface localization (PR #20): `Resources/Localization/SharedResource.ru.resx`, Russian as translation target, System language preference.
- Learning repeat/direction clarity (PR #21): Learn card exposes direction and `IsAgainRepeat`, visual badges for legitimate repeats.
- Beta 11 release identity and What's New (PR #22): version/build to `1.0.0-beta.11`/`11`.
- Beta 12 Russian-translation and UI fixes (PR #23, merged as PR #32): version/build to `1.0.0-beta.12`/`12`; export verification fix, import UI fixes, workflow-state notifier, online-lookup consent UI.

## Design work completed but not yet implemented

- **KF-BACKUP-002 Slices 1–3** (merged on `feature/backup-merge-contracts-v1`, `feature/backup-merge-safety-copy-v1`, `feature/backup-merge-preflight-v1`): merge contracts library, safety-copy service, and read-only merge preflight — awaiting staged merge and eventual populated-target merge writer (Slice 5+).
- **KF-MEANING-001 Slice 0** + **Slice 0.1** (merged as `feature/meaning-centric-architecture-v1`): binding architecture decision, activation-boundary review, dormancy rules, nine-slice implementation sequence. **Slice 1** (merged as `feature/meaning-centric-dormant-migration-v1`): dormant Schema 7→8 migration. **Slice 2** (merged as `feature/meaning-centric-archive-v2`): archive format v2 and dual-schema backup. **Slice 3** (current branch `feature/meaning-centric-multi-sense-preparation-v1`, committed as `a51b0e82e35b2bb82d707580fe4fa04f8ac79765`, PR #33 open, not yet merged): multi-Sense preparation and topic persistence, dual-schema compatible.

## Current known risks and limitations

- Slice 3 test coverage: no single test exercises all four semantic discriminators (`provider-sense-id`, `topic/domain`, `grammatical-relationship`, `no-discriminator`) in one combined lookup (pairwise/sequential coverage complete; unified scenario not tested). See [kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md) §F.
- Slice 3 edit-API behavior: edited content not reappearing is structurally guaranteed by evidence-ledger formula but not tested by a dedicated edit-scenario test.
- Slice 3 checkpoint naming: `BeforeCandidateCompletion` vs. `BeforeAutomaticCandidateCompletion` distinction is clear but could benefit from naming review.
- Slice 3 duplicate evidence across documents — two distinct concerns, not one, corrected this pass (see [kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md) §F.4 for the full analysis): (1) the **evidence ledger** (`Schema8EvidenceScanner`/`Schema8EvidenceLedger`, keyed by the four-field `ContextEvidenceKey`) correctly treats identical normalized text in two different documents as distinct evidence — `SourceDocumentId` is part of the key — proven structurally by the code, not by a dedicated isolated test. (2) The **`ContextSnapshots` display rows** persisted for a given Meaning intentionally deduplicate by `NormalizedFingerprint` alone, scoped per `MeaningId` (`PreparationServiceSchema8.InsertNewContextSnapshots`), matching the pre-existing, unchanged `IX_ContextSnapshots_Meaning_Fingerprint` unique index (`Data/Entities/ContextSnapshotEntity.cs`) — so if identical normalized text from two different documents is linked to the same Meaning, only one illustrative snapshot row is kept. This does not lose evidence identity: eligibility scanning reads the four-field key from the candidate envelope's `FrozenEvidence` (and the ledger's legacy-baseline fallback), never solely from which `ContextSnapshots` rows happen to exist.
- Slice 3 envelope-upgrade performance: the lazy upgrade read/transaction path has not been benchmarked against prior code paths.

## Active planned sequence

1. **Slice 3 merged**: committed as `a51b0e82e35b2bb82d707580fe4fa04f8ac79765`, merged into master via [PR #33](https://github.com/Tachiguro/KnownFirst/pull/33) (merge commit `b405aa49001538ac60f45dc9697d9308e48e9eb2`).
2. **Slice 4** (design done, not started): Sense answer-variant assignments, per-card/per-variant progress, synonym-credit replay.
3. **Slice 5** (design done, not started): Sense-addressed cards, learning queue, per-Sense mastery rollup.
4. **Slice 6 — Schema 8 activation** (design done, not started): the one-way flip of `CurrentVersion` to `8`, wiring the migration into `InitializeAsync`. Depends on Slices 1–5 merged.
5. **Slice 7** (design done, not started): MergePreflight adaptation for Sense-addressed queries.
6. **Slice 8** (design done, not started): populated-database merge writer and Import routing. Depends on Slice 6.
7. **Slice 9** (design done, not started): Import UI and convergence testing.

## Explicit boundaries for Slice 3

- Slice 3 diff must not touch: `Data/DatabaseSchema.cs`, `DatabaseSchema.CurrentVersion`, `DatabaseSchema.InitializeAsync`, `Data/Entities/*`, Schema8Ddl, `Components/Pages/PrepareWords.razor`, localization, archive DTOs/format, `LearningService`, answer-variant/assignment/progress entities, review fields, merge writer, Import UI, package identity, build number, secrets, branding.
- **Verified:** none of these are changed by the committed Slice-3 diff (`git diff master...HEAD --name-status`).

## Relevant files for Slice-3 review

- **Handoff:** [docs/handoffs/kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md)
- **Architecture:** [docs/architecture/meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) §2–§4
- **Added services (10):** `Services/Study/PreparationSchemaCapability.cs`, `PreparationServiceSchema8.cs`, `PreparationServiceSchema8Start.cs`, `PreparationCandidatePayloadV1.cs`, `PreparationCandidatePayloadCodec.cs`, `PreparationCandidateStateException.cs`, `PreparationMetadataPolicy.cs`, `PreparationSenseClassifier.cs`, `Schema8EvidenceLedger.cs`, `Schema8EvidenceScanner.cs`
- **Added core library (1):** `KnownFirst.Core/Preparation/PreparationContextEvidencePolicy.cs`
- **Modified services:** `Services/Study/PreparationService.cs` (real Schema-8 dispatch, lazy upgrade, multi-Sense logic)
- **Added tests (9):** `PreparationSchemaCapabilityTests.cs`, `PreparationContextEvidencePolicyTests.cs`, `PreparationServiceSchema8StartAndEvidenceTests.cs`, `PreparationServiceSchema8LazyUpgradeTests.cs`, `PreparationServiceSchema8AcceptTests.cs`, `PreparationMetadataPolicyTests.cs`, `PreparationProviderIndexIntegrityTests.cs`, `PreparationSenseClassifierTests.cs`, `PreparationCandidatePayloadCodecTests.cs` — covering schema-capability, evidence, codec, metadata, sense classification, provider-index integrity, lazy upgrade, acceptance logic.
- **Modified test infrastructure:** `KnownFirst.Tests/TestInfrastructure.cs` (synthetic Schema-8 fixture support — modified, not added).

## Next exact action

**Windows GUI-test launcher foundation, reconciled with Slice 3 master:**

1. KF-MEANING-001 Slice 3 is merged into master ([PR #33](https://github.com/Tachiguro/KnownFirst/pull/33), merge commit `b405aa49001538ac60f45dc9697d9308e48e9eb2`).
2. The Windows GUI-test launcher foundation (branch `feature/windows-gui-test-launcher-v1`, originally committed as `2cbfa7f3221de45a7b2cd8cbd021742068180b09`) has been merged with and reconciled against that master via a local merge commit (`merge: reconcile GUI launcher with Slice 3 master`). Only documentation was adjusted during reconciliation; no production, script, or test code changed.
3. Remaining action: one real manual `StartupSmoke` execution through `scripts/knownfirst.ps1` (`-Action GuiTest -GuiScenario StartupSmoke -Configuration Debug`) to verify the launcher against the reconciled master, followed by human review.
4. Rendered GUI interaction automation (clicking controls, sending input, comparing rendered layouts) remains a separate, not-yet-started future package; StartupSmoke stays process/window/startup-event verification only.

## Design work (not implemented)

- **KF-BACKUP-002** (P1, blocks public release readiness, does not block Beta 12 Internal Testing): non-destructive portable archive merge design, revision 2, recorded in [architecture/backup-merge-v1-design.md](architecture/backup-merge-v1-design.md) and [BACKLOG.md](BACKLOG.md). Revision 2 corrected source-material/Meaning/LearningCard identity against verified code, added a mandatory pre-merge safety copy, an explicit clock-skew/event-fingerprint policy, and idempotency proofs; four open decisions remain for product/engineering sign-off (design doc §13).
  - **KF-BACKUP-002 Slice 1 — pure merge contracts** (branch `feature/backup-merge-contracts-v1`, uncommitted): implemented design §12 slice 1 — a database-independent library under `Services/DataSafety/Merge/` (`KnownFirst.Services.DataSafety.Merge` namespace) providing the canonical SHA-256 byte-encoding contract, every §4 stable-identity function (SourceMaterial, Vocabulary, Meaning, LearningCard matching, ReviewSession/Candidate, PreparationSession/Candidate, LearningSession/Card), the §6 LearningReview event fingerprint, and every §5 conflict-resolution matrix (KnowledgeState, PreparationState, LearningCard-state classification, workflow-session status), plus pure fixture/set utilities used to prove the §11 idempotency/convergence scenarios. No SQLite access, no service orchestration, no import writer, no UI — see design doc's new "Product contract" section for the resulting single-Import-action behavior this and later slices implement. Tests added under `KnownFirst.Tests/Merge*Tests.cs`.
  - **KF-BACKUP-002 Slice 2 — validated merge safety copies** (branch `feature/backup-merge-safety-copy-v1`, uncommitted): implemented design §12 slice 2 — `MergeSafetyCopyService` (`Services/DataSafety/Merge/MergeSafetyCopyService.cs`) creates a private, validated pre-merge recovery archive. A new `BackupSnapshotRepository.CapturePortableSnapshotForMergeSafetyCopy` performs the active-workflow check (Review/Preparation/Learning) and the portable snapshot capture inside one `ExecuteSnapshotAsync` transaction, so a safety copy can never omit an active workflow to a time-of-check/time-of-use gap; Slice 2 guarantees only that the safety-copy snapshot itself was captured with no active workflow — a future merge writer must re-check the condition again immediately before mutation. Storage root is derived solely from `IKnownFirstDatabase.DatabasePath`'s parent directory (`<database-directory>/merge-safety-copies/`), never from `FileSystem.AppDataDirectory`/Documents/Downloads, so a GUI-test-profile database keeps its safety copies inside that isolated profile automatically. Archive creation reuses the existing `BackupSnapshotRepository`/`BackupModelMapper`/`BackupArchiveWriter`/`BackupArchiveReader.ValidateAsync` pipeline unchanged (no second format); the archive is staged, validated, moved to a unique final `.kfarchive` path, and re-validated from that final path before a private `.metadata.json` sidecar (source-generated JSON) is staged, finalized, and re-read-validated — only after both are valid does an older recognized safety-copy pair get removed (best-effort, never for unrecognized files). Failure/cancellation always retains the previously valid pair and cleans up only the current attempt, returning `BlockedByActiveWorkflow`/`Cancelled`/`SafetyCopyFailed` with the existing stable codes (`active-workflow-unsupported`/`operation-cancelled`/`safety-backup-failed`). No merge matcher, merge writer, Import routing, UI, schema, or archive-format change. Tests added in `KnownFirst.Tests/MergeSafetyCopyServiceTests.cs`.
  - **KF-BACKUP-002 Slice 3 — read-only merge preflight** (branch `feature/backup-merge-preflight-v1`, uncommitted): implemented design §12 slice 3 — a pure `MergePreflightPlanner` (`Services/DataSafety/Merge/MergePreflightPlanner.cs`) computes the complete, deterministic merge preflight plan from a target and an archive `BackupPayload` plus the archive's `BackupManifest`, using only Slice 1's stable-identity/conflict policies (no SQLite, filesystem, network, environment, current-time, random, Preferences, or MAUI dependency, no write transaction). A read-only `MergePreflightService` (`Services/DataSafety/Merge/MergePreflightService.cs`) validates the archive via the existing `BackupArchiveReader`, captures the target with the same race-free `BackupSnapshotRepository.CapturePortableSnapshotForMergeSafetyCopy` call Slice 2 already uses (fail-closed active-workflow classification reused unchanged), maps the snapshot through the existing `BackupModelMapper`, and invokes the planner — it creates no safety copy and opens no write transaction. The plan (`Services/DataSafety/Merge/MergePreflightModels.cs`) exposes seven statuses (`Ready`/`NoChanges`/`RequiresUserDecision`/`BlockedByActiveWorkflow`/`ValidationFailed`/`Cancelled`/`Failed`), per-entity-kind counts across six classifications (`New`/`ExactDuplicateSkipped`/`Enriched`/`PreservedVariant`/`UnresolvedConflict`/`DeduplicatedEvent`) for all 16 portable entity kinds, exact itemized plan actions keyed only by stable identities and archive-local ids (never target-local numeric ids), same-tier `KnowledgeState` conflict decisions, required `PreferredMeaningConflict` decisions (target/archive Meaning summaries plus `KeepTargetMeaning`/`UseArchiveMeaning`/`KeepBothSelectTarget`/`KeepBothSelectArchive` choices, never auto-applied), a `RequiresSchedulerReplay` flag, sample details bounded to 20 per classification, and stable warning/error codes. Tests added in `KnownFirst.Tests/MergePreflightPlannerTests.cs` and `KnownFirst.Tests/MergePreflightServiceTests.cs`; `KnownFirst.Tests/MergePreflightFixtures.cs` holds the shared fixture builders. No safety-copy creation, SQLite mutation, scheduler replay, merge writer, Import routing, UI, or archive-format change.
    - **Revised** (same branch) for the approved meaning-centric model (design doc §16, backlog [KF-MEANING-001](BACKLOG.md)): added `SemanticMeaningIdentity`/`ExactMeaningVariantIdentity`/`AnswerVariantIdentity`/`FutureCardIdentity` (`Services/DataSafety/Merge/SemanticMeaningIdentities.cs`); the planner now classifies `PreparedMeaning` by SemanticMeaning/exact-variant identity instead of the old row-level `MeaningIdentity`, and gives SentenceRange/Occurrence/EncounteredForm/LegacyReviewSummary/ContextSnapshot their own stable-parent-based identities (no more parent-lockstep classification). Tests added/revised in `KnownFirst.Tests/MergeSemanticMeaningIdentityTests.cs` and extended `MergePreflightPlannerTests.cs`.
    - **Focused correction pass** (same branch, design doc §17): `SemanticMeaningIdentity` no longer hashes `Translation` (it was incorrectly splitting synonyms into separate meanings); `ExactMeaningVariantIdentity` gained the `Translation` field it was missing entirely; the planner now actually matches `LearningCardEntity` rows by `FutureCardIdentity` (not `(VocabularyIdentity, Direction)`), with the physical identity retained only to detect a same-slot collision between two distinct SemanticMeanings, which is now a plain `New` card (never `PreservedVariant`) plus the new blocking prerequisites `meaning-card-schema-migration-required` and `archive-format-migration-required` (the latter verified directly against `BackupArchiveWriter.ValidatePayloadGraph`'s card-uniqueness check). `PreferredVariantSelectionDecision` is now blocking. `AnswerVariant` was removed as a `MergeEntityKind`; answer-variant plans are now exposed separately via `MergePreflightPlan.DerivedAnswerVariantPlans` and never affect primary per-entity counts. Added a blocking `SemanticMeaningGroupingDecision` for ambiguous same-Word/no-discriminator/differing-Translation cases. Added a new `BlockedByPrerequisite` status and `IsExecutable`/`BlockingPrerequisites` fields on the plan. `VocabularyReviewItem`/`PreparationItem`/`LearningQueueItem` now compare full historical content (preserving divergent history as an additional row) instead of collapsing on identity alone; `VocabularyReviewWorkflow` content divergence for the same document is a new blocking `workflow-history-schema-migration-required` case. The review-event fingerprint used inside the planner is now meaning-aware (`FutureCardIdentity`-keyed), distinct from Slice 1's unchanged persisted `LearningReviewFingerprintPolicy`.
    - **Final focused review correction** (same branch, design doc §18): `SemanticMeaningIdentity`/`ExactMeaningVariantIdentity` bumped to `.v3` — `Definition` had the identical defect §17 already fixed for `Translation`, one field over: it was still hashed unconditionally into `SemanticMeaningIdentity` and treated as a reliable discriminator on its own, so a same-provider-sense-id pair with merely differently-worded definitions was silently misclassified as two distinct SemanticMeanings, and a no-discriminator pair with differing Definition wording was silently auto-split instead of raising a `SemanticMeaningGroupingDecision`. `Definition` is now excluded from `SemanticMeaningIdentity`/`HasReliableSenseDiscriminator` (symmetric with `Translation`) and hashed directly into `ExactMeaningVariantIdentity`. Fixed the four affected `MergePreflightPlannerTests`/`MergeSemanticMeaningIdentityTests` fixtures that had relied on the old (incorrect) "Definition presence alone is reliable" behavior, and added coverage for same-provider-sense-id/differing-Definition, differing-provider-sense-id, no-discriminator/differing-Definition, matching-Definition-without-a-real-discriminator, and alias-reordering-never-creates-a-preferred-variant-decision. 8 tests added net (1003 → 1011 complete-suite total; 337/337 focused Slice 1–3 merge/portable-archive tests). Also corrected this document's stale top-level "Not implemented" status line (Slices 1–2 are merged; Slice 3 is implemented only on this unmerged branch).
- **KF-MEANING-001 Slice 0 — binding meaning-centric architecture decision** (branch `feature/meaning-centric-architecture-v1`, uncommitted, architecture-only): [architecture/meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) decides Option B (`WordEntity` → `SenseEntity` → `MeaningEntity`/exact variants → `AnswerVariantEntity` → `AnswerVariantProgressEntity`) as binding, not provisional, after a code audit found no blocking incompatibility. Defines the full Schema 8 entity model (all changes additive to existing tables — no column removed), the binding answers for all twelve required decisions (Sense fields/identity/status, Sense-knowledge-on-SenseEntity with no separate `SenseKnowledgeEntity`, Meaning/Sense field split, AnswerVariant roles, AnswerVariantProgress fields, `(SenseId, Direction)` card identity with `WordId`/`MeaningId` retained temporarily as legacy metadata, Sense-owned contexts with optional Meaning/Document provenance, the end-to-end topic/domain persistence path, Word-vs-Sense status, and the three sense-independence/provider rules), the Schema 7→8 migration contract (per-word Sense grouping, fail-closed missing/ambiguous/corrupt-reference handling, atomicity via the existing `RunInTransactionAsync` mechanism, idempotency/repeated-initialization, rollback, future-schema rejection), the archive v1↔v2 compatibility contract (manifest version bump alone is sufficient, `.kfarchive` retained, v1 upgrades in memory using the identical grouping algorithm as the live migration, `cardKeys` becomes `(SenseId, Direction)`-keyed for v2), and an 8-slice implementation sequence with explicit dependencies and acceptance criteria, none of which may combine schema migration, archive migration, learning behavior, and UI in one PR. **No entity, schema, service, archive DTO, or UI code was changed by this slice** — `BACKLOG.md`, `CURRENT_WORK.md` (this entry), `docs/plans/structured-vocabulary-import-and-sense-learning.md`, and `docs/architecture/backup-merge-v1-design.md` were updated minimally to mark the prior provisional Option-B recommendation as decided and cross-reference this document.
  - **Focused correction pass** (same branch, still uncommitted, architecture-only): corrected four defects in the Slice 0 design doc. (1) `WordEntity.Status` no longer conflates authoritative word-review state (`Unreviewed`/`Known`/`UnknownBacklog`/`Ignored`) with a Sense-learning rollup — migration resets a word's `Status` to `UnknownBacklog` when its legacy `Prepared`/`Learning`/`Mastered` value is relocated to the new Sense row, and no Schema-8-or-later code ever writes those three values to `WordEntity.Status` again; any word-level learning summary is computed live from Sense rows, never persisted. (2) `AnswerVariantEntity`'s three-way `Role` enum (`PrimaryAnswer`/`RequiredAnswerVariant`/`AcceptedAlias`) is replaced by two orthogonal facts: `SenseEntity.PreferredAnswerVariantId` (singleton preferred-display pointer) and `AnswerVariantEntity.Requirement` (`Required`/`AcceptedOnly`) — a preferred answer may also be required. (3) `LearningSessionCardEntity.TargetAnswerVariantId` and `LearningReviewEntity.TargetAnswerVariantId`/`MatchedAnswerVariantId` (nullable) were added, with a defined synonym-credit rule (a correct-but-different accepted synonym credits only the matched variant, never the targeted one, which remains pending) and a reactivation rule (promoting a new required variant can un-retire a mastered Sense). (4) A three-tier stable-identity taxonomy was added (local SQLite numeric ID / content-derived semantic matching key / immutable `StableId`), with a new `StableId` column on `SenseEntity`/`MeaningEntity`/`AnswerVariantEntity` so mutable content (definition, translation, topic, preferred-answer text) is never a row's sole permanent identifier. The Schema 7→8 migration section was restructured into an explicit 9-step atomic order and the alias-language rule was resolved by code inspection (`LearningService.CheckSpellingAsync` compares typed input against `meaning.DisplayTerm`+aliases+`word.Language` unconditionally regardless of `card.Direction`, confirmed by the `Prepare_AcceptedAliases` = "Accepted spelling aliases" UI label) rather than left as an assumption. `BACKLOG.md` and `docs/architecture/backup-merge-v1-design.md` were updated minimally to reflect the correction; no entity, schema, service, archive DTO, or UI code was changed.
  - **Second focused correction pass** (same branch, still uncommitted, architecture-only): corrected five further defects, all downstream of one root issue — preferred-display and mastery-requirement were still modeled as **Sense-wide** facts when both are actually **direction-specific**. (1) `AnswerVariantEntity` no longer carries `Requirement`; it is now a pure normalized answer expression (`DisplayText`/`NormalizedText`/`AnswerLanguage`). A new `SenseAnswerVariantAssignmentEntity`, unique by `(SenseId, CardDirection, AnswerVariantId)`, owns `Requirement` (`Required`/`AcceptedOnly`) and `IsPreferred` per direction — the same variant can be `Required` for `MeaningToTerm`, merely `AcceptedOnly` for `TermToMeaning`, absent from a direction entirely, or preferred and required simultaneously. The singleton "one preferred assignment per `(SenseId, CardDirection)`" rule is enforced by a raw SQLite partial unique index (`WHERE IsPreferred = 1`), not merely an app-level check, with a concurrency test required in Slice 1. `AnswerLanguage` must never be used to infer `CardDirection` (source/explanation languages may match). (2) `SenseEntity.PreferredMeaningId` is renamed `DefaultMeaningId` and downgraded to a non-authoritative fallback; each `LearningCardEntity` gains its own authoritative `PreferredMeaningId`, migrated in place from the legacy `MeaningId` column (renamed, not left as disposable legacy metadata) — `TermToMeaning` and `MeaningToTerm` cards for the same Sense may now legitimately prefer different exact Meaning variants. (3) `AnswerVariantProgressEntity` is now unique by `(CardId, AnswerVariantId)`, equivalent to `(SenseId, CardDirection, AnswerVariantId)` since `LearningCard` is unique by `SenseId`+`Direction` — the synonym-credit rule now only credits a matched variant when it is itself a valid assignment for that same card, and mastery/retirement is evaluated per card (per direction), with Sense-level `Mastered` rolling up only once every existing card for the Sense is `Retired`. (4) `WordEntity.PreparationState`'s word-vs-sense scoping (previously deferred) is now resolved: a technical workflow/cache field only, never semantic-completeness state; a word already `PreparationState.Prepared` still surfaces a genuinely new candidate sense from later document context, matched against existing Senses by the same matching-key policy the migration uses, with frequency affecting priority only, never suppression; active preparation sessions migrate untouched since the migration never reads/writes them. (5) The Schema 7→8 migration's atomic order gained the `LearningCards.MeaningId → PreferredMeaningId` column rename and the assignment-generation sub-steps. `BACKLOG.md`, `docs/plans/structured-vocabulary-import-and-sense-learning.md`, and `docs/architecture/backup-merge-v1-design.md` were updated minimally; no entity, schema, service, archive DTO, or UI code was changed.
  - **Slice 0.1 — activation-boundary and compatibility-sequencing review** (branch `docs/meaning-centric-schema-activation-sequence-v1`, uncommitted, documentation-only): audited the confirmed activation concern that `LearningService` still reads/writes automatic-progression counters and `WordStatus.Learning`/`Mastered` on `WordEntity` (`LearningService.cs:524,635,641,661-688`) and retires every `LearningCard` sharing `WordId` regardless of Sense (`LearningService.cs:690-711`), and that `PreparationService.AcceptAsync` still guards candidate selection by Word-level confirmed-Meaning presence and `PreparationState` (`PreparationService.cs:100-104,344-348`) — verified these are still exactly the shape of the live code, unchanged since Slice 0. Verified a further, code-grounded reason activation cannot be schema-only: the migration's `LearningCards.MeaningId → PreferredMeaningId` rename (§2.7/§4.2 step 8) breaks `PreparationService.cs:429`, `LearningService.cs:111,422`, and `Data/BackupImportRepository.cs:235` at the SQL layer the instant a real database migrates, so the rename, the `CurrentVersion` flip, and the minimum service/backup-repository adaptations are one indivisible unit. Evaluated activating Schema 8 immediately with a "minimum" compatibility bridge in the same PR (stop writing `WordStatus.Learning`/`Mastered` and the frozen automatic-progression columns, create a Sense per Meaning, populate `SenseId`/`PreferredMeaningId`) and rejected it: a working replacement for mastery/automatic-progression tracking is a precondition for stopping the legacy writes without regressing behavior for existing users, and that replacement is itself the substantial content of the design doc's own Slices 3–4 — bundling it into a first-ever, first-touches-real-data PR contradicts the already-binding "no slice combines schema migration, archive migration, learning behavior, and UI" rule. Adopted and documented binding strategy `DormantFoundationThenActivation`: [meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) §4.8 (new) states the three-way activation boundary (schema/migration code existing in the repository / callable only by tests / activated for real application databases) and the mandatory compatibility bridges; §6's slice table is revised so Slices 1–5 stay dormant (`DatabaseSchema.CurrentVersion` remains `7`, `InitializeAsync` never invokes the migration) and a new Slice 9 (**Activation**) is the only slice permitted to flip `CurrentVersion` to `8`, wire the migration into `InitializeAsync`, and transform a real user database — depending on Slice 1 (migration), Slice 3 (Sense creation), and Slice 4 (mastery/automatic-progression replacement), with Slice 6 (MergePreflight adaptation) now depending on Slice 9 instead of Slice 1/2 directly. Archive v2 (Slice 2) may ship before or after Activation since archive v1 stays fully readable/writable in the interim per the existing §5.1 in-memory-upgrade contract. `docs/BACKLOG.md` and this entry were updated minimally to record the correction; **no entity, schema, service, archive DTO, or UI code was changed.**
  - **Slice 0.1 — final focused correction** (same branch, still uncommitted, documentation-only): corrected four defects in the activation-boundary review above, none of which change the entity model. (1) **Dormancy was underspecified:** "`DatabaseSchema.CurrentVersion` stays 7" alone is not sufficient, because sqlite-net's `CreateTableAsync<T>()` silently issues `ALTER TABLE ... ADD COLUMN` for any new property on an already-`InitializeAsync`-registered entity class, regardless of `CurrentVersion` — a dormant Slice 1 must add zero columns to the existing, live-mapped entity classes and zero new tables to the normal `InitializeAsync` sequence, using migration-specific models/raw SQL instead, or a "dormant" foundation would still silently alter every real Schema-7 database's tables on ordinary startup. (2) **Slices 3–5 were only vaguely "dormant":** corrected to a precise dual-schema rule — each must be either completely unreachable from normal execution or explicitly version-gated (Schema-7 behavior remains default at `user_version = 7`; Schema-8 behavior only at `8`), with both a Schema-7 regression suite and a Schema-8 fixture suite, and schema-capability decisions must read the validated `PRAGMA user_version`, never merely an optional column's existence. (3) **Archive v2 sequencing was backwards:** the prior pass said archive v2 "may ship before or after Activation"; this is wrong and now corrected — archive v2 must precede activation, because the migration's own split-not-merge Sense-grouping (§4.2 step 3) can produce two `SenseEntity` rows for one Word, hence two same-`(Word, Direction)` cards, from the very first real migration run, and archive v1's `cardKeys` uniqueness check cannot represent that; safety copy, export, validation, and empty Restore must therefore already support the Schema-8 graph (as v2) by the time Schema 8 activates. (4) **Slice numbering was confusing** (a "Slice 9" inserted before Slices 6–8): renumbered to a coherent, gapless sequence — Slice 0 (binding architecture), Slice 0.1 (activation boundary), Slice 1 (dormant migration engine), Slice 2 (archive v2 + backup-repository dual-mode), Slice 3 (multi-Sense preparation, dual-schema), Slice 4 (answer assignments/progress replay, dual-schema), Slice 5 (Sense-addressed cards/queue, dual-schema), Slice 6 (Schema-8 activation — depends on all of Slices 1–5, no exceptions), Slice 7 (MergePreflight adaptation, may follow activation since it is read-only), Slice 8 (populated-database merge writer and Import routing), Slice 9 (UI and end-to-end convergence validation). [meaning-centric-learning-v1-design.md](architecture/meaning-centric-learning-v1-design.md) §4.8 (restructured into §4.8.1–§4.8.5) and §6 (slice table) carry the correction; `docs/BACKLOG.md` and this entry were updated minimally. **No entity, schema, service, archive DTO, or UI code was changed.**
  - **Slice 1 — dormant Schema 7→8 migration engine and synthetic migration tests** (branch `feature/meaning-centric-dormant-migration-v1`, uncommitted, implemented locally): implemented the §4 migration contract as `Schema8DormantMigration.ApplyAsync(SQLiteAsyncConnection)` in the isolated `KnownFirst.Data.Migrations.Schema8` namespace (`Data/Migrations/Schema8/`), callable only from tests. Strictly dormant per §4.8.1: `Data/DatabaseSchema.cs` is byte-for-byte unchanged, `CurrentVersion` stays `7`, `InitializeAsync` never references the migration, and the new Schema-8 tables/columns/rename exist only inside the migration's own raw-SQL steps — proven by a dedicated test that an ordinary `InitializeAsync` run creates zero Schema-8 tables/columns/indexes and leaves `user_version` at `7`. The migration runs the full §4.3 nine-step order inside one `RunInTransactionAsync` transaction (fault-injectable at every step boundary via a test-only `Schema8MigrationOptions.FaultInjectionHook`, proven to roll back completely — schema, indexes, version, and rows — after table creation, after backfill, and after old-index removal but before the version write), rejects a future version (`> 8`) or an unsupported earlier version (`≠ 7`) without touching the database, and treats an already-`8` database as an idempotent no-op after validating its shape. Sense grouping reuses the existing `SemanticMeaningIdentityPolicy`/`HasReliableSenseDiscriminator` unchanged (`Services/DataSafety/Merge/SemanticMeaningIdentities.cs`): two Meanings merge into one Sense only when both carry a reliable discriminator (provider sense id, topic/domain, grammatical relationship, or acronym expansion) and their computed identity agrees; every other case — including two Meanings with differing Definition/Translation wording and no discriminator — splits into separate Senses rather than blocking or guessing. The bundled SQLite (verified at test time: `3.53.3`, via `sqlite_version()`) supports `ALTER TABLE ... RENAME COLUMN` (available since 3.25.0), so `LearningCards.MeaningId → PreferredMeaningId` uses that fast path by default; the documented transactional table-rebuild fallback is fully implemented and separately proven correct via a test-only `Schema8MigrationOptions.ForceColumnRebuildFallback` flag. The `SenseAnswerVariantAssignments` partial unique index (`(SenseId, CardDirection) WHERE IsPreferred = 1`) is proven to reject a second preferred assignment on the same connection and, via a `Barrier`-synchronized two-connection race (no sleeps), to admit exactly one committed winner under real concurrent contention. 29 tests added (`KnownFirst.Tests/Schema7Fixture.cs`, `Schema8MigrationAssertHelpers.cs`, `Schema8DormantMigrationCoreTests.cs`, `Schema8DormantMigrationFocusedTests.cs`, `Schema8DormantMigrationFaultInjectionTests.cs`, `Schema8DormantMigrationConcurrencyTests.cs`). `KnownFirst.Tests.csproj` gained one `Compile Include` line for the new production-side migration files (the test project has no `ProjectReference` to the main app and links production sources file-by-file, same pattern as every other production file it already references). No entity, schema, service, archive DTO, or UI code was changed; `Data/DatabaseSchema.cs` and every file under `Data/Entities/` are unchanged.
  - **Focused correction (same branch, uncommitted): grouped-Sense AnswerVariant losslessness** — fixed a confirmed defect where `Schema8DormantMigration`'s step 2 generated term-side/explanation-side `AnswerVariant` rows only from each Sense's *representative* Meaning, leaving every other grouped Meaning's `DisplayTerm`/`Translation` as inert, never-usable Meaning-row text (e.g. Meaning A `Translation = "Bank"` and Meaning B `Translation = "Kreditinstitut"` grouped into one Sense — only "Bank" became an `AnswerVariant`). Corrected to iterate every Meaning row assigned to the Sense, collecting each one's non-empty `DisplayTerm` (`AnswerLanguage` = source language), non-empty `Translation` (`AnswerLanguage` = explanation language), and every accepted alias (unchanged from before), deduplicated by the existing `(SenseId, AnswerLanguage, NormalizedText)` unique index via the existing `GetOrCreateAnswerVariant`/`EnsureAssignment` helpers — so the representative Meaning's own already-created preferred assignment is revisited as a safe no-op (dedup hits the existing row) and every other Meaning's expressions become new `AcceptedOnly`, non-preferred assignments (never `Required`, never a second preferred row). One regression test added (`Schema8DormantMigrationFocusedTests.GroupedSense_EveryMeaningsDisplayTermAndTranslation_BecomeUsableAnswerVariants`, the exact Bank/Kreditinstitut scenario). Complete suite now 1044/1044 (1043 + 1). No entity, schema, service, archive DTO, or UI code was changed; `Data/DatabaseSchema.cs` and every file under `Data/Entities/` remain unchanged.
  - **Final focused invariant review and correction (same branch, uncommitted)** — corrected two further defects the losslessness fix above did not fully address, neither changing the entity model. (1) **Nondeterministic `SourceMeaningId`:** the prior pass's per-Meaning loop iterated `group` (and the `Meanings` table read) without an explicit order, so which contributing Meaning "won" `SourceMeaningId` for a duplicated expression depended on database-return/grouping-insertion/incidental loop order rather than a stated rule. Corrected: `Schema8DormantMigration` now reads `Meanings` with `ORDER BY Id` and additionally re-sorts every group ascending by `Meaning.Id` at the point of use before any provenance decision — since the Sense's representative/default Meaning is itself always the group's lowest-`Id` Meaning, "representative wins when it contributed" and "otherwise lowest legacy `Meaning.Id` wins" collapse into one explicit, uniform rule (ascending-Id, first-writer-wins dedup) rather than two separately-reasoned cases. (2) **Missing preferred assignment when the representative contributes nothing for a direction:** the prior pass only ever attempted to mark the *representative* Meaning's own expression preferred for a direction; when the representative had no `Translation` but another grouped Meaning did (and a `TermToMeaning` card existed), the direction ended up with only `AcceptedOnly` assignments and **zero** preferred assignments — violating "exactly one preferred assignment per existing card direction with an answer." Corrected via a new `CreatePreferredAssignmentForDirection` helper: for each direction, the candidate pool is every Meaning in the group with a non-empty expression for that direction; the preferred one is chosen by the same deterministic order (lowest `Meaning.Id`, tie-broken by lowest normalized text ordinally — the tie-break is unreachable in practice since `Meaning.Id` is unique, but implemented for completeness), never inventing a preferred assignment when no Meaning in the group has any expression for that direction. Aliases remain categorically excluded from ever being the preferred candidate and never displace it (Focused invariant 3, already the existing behavior, now covered by a dedicated non-representative-alias test). 8 tests added (3 determinism, 4 preferred-assignment-fallback including a re-verified partial-unique-index rejection, 1 non-representative-alias); one existing test (`GroupedSense_EveryMeaningsDisplayTermAndTranslation_BecomeUsableAnswerVariants`) updated in place since its old assertion ("TermToMeaning is never preferred") encoded the very defect just fixed. `docs/architecture/meaning-centric-learning-v1-design.md` §4.2 steps 6/6a/7 corrected narrowly to state the same rules (all grouped Meanings contribute candidates; dedup by `(SenseId, AnswerLanguage, NormalizedText)`; deterministic `SourceMeaningId`; matching-direction assignments are `AcceptedOnly`; the representative-derived preferred assignment is retained; a deterministic fallback is selected only when the representative contributes nothing for an existing direction; no `Required` assignment is ever invented) — no other section rewritten. Focused Schema8 suite now 38/38; the complete suite was not rerun this pass (last known good: 1044/1044, unaffected by this internal-only correction). No entity, schema, service, archive DTO, or UI code was changed; `Data/DatabaseSchema.cs` and every file under `Data/Entities/` remain unchanged.

  - **Slice 2 — archive format v2 and dual Schema-7/Schema-8 backup-repository support** (branch `feature/meaning-centric-archive-v2`, uncommitted, implemented locally): implemented the §5 archive v2 contract and the §4.8.2/§4.8.4 dual-schema backup bridge. `Data/DatabaseSchema.cs`, every file under `Data/Entities/`, `CurrentVersion` (`7`), and `Components/Pages/Settings.razor` are all byte-for-byte unchanged; no preparation/learning service, MergePreflight, populated-database merge writer, or Import UI code was touched.
    - **Trusted schema-capability component** (`Services/DataSafety/BackupSchemaCapability.cs`): reads `PRAGMA user_version`, accepts exactly `7`/`8`, validates the matching physical shape (reusing `Schema8DormantMigration`'s shape checks via a new shared `Schema8ShapeValidator`), and fails closed otherwise. Returns one of two distinct top-level capability types (`ValidatedSchema7Capability`/`ValidatedSchema8Capability`, `internal` constructors) that the v1/v2 writers require by parameter type — passing a Schema-8-derived capability to the v1 writer (or vice versa) is a compile error, not a runtime case to defend against.
    - **Archive format v2**: new DTO root (`Models/BackupModelsV2.cs`) fully separate from v1 — `BackupSense`, `BackupPreparedItemV2` (+`SenseId`/`StableId`), `BackupContextSnapshotV2` (+explicit `SenseId`), `BackupAnswerVariant`, `BackupSenseAnswerVariantAssignment`, `BackupAnswerVariantProgress`, `BackupLearningCardV2` (no `PreparedItemId`), `BackupLearningReviewV2`/`BackupLearningQueueItemV2` (+target/matched variant references), `BackupRecordCountsV2`, `BackupManifestV2`, `BackupPayloadV2`, plus the peek-only `BackupFormatVersionEnvelope`. New source-generated-only JSON contract (`BackupJsonSerializerContextV2`, `BackupJsonCodecV2`) and a bounded-duplication field validator (`BackupModelContractV2`, deliberately not sharing internals with the unmodified `BackupModelContract`). New `BackupArchiveWriterV2` (counts derived only from the payload being serialized, never a second source) and `BackupArchiveWriterV2.ValidatePayloadGraphV2` implementing every v2 graph invariant (unique ids/StableIds per collection, Sense/Meaning/AnswerVariant/Assignment reference and agreement rules, Context↔Sense agreement, ≤1/exactly-1 preferred-per-direction, card/review/queue/progress reference-scoping, and a Schema-8 Word-status invariant rejecting legacy `Prepared`/`Learning`/`Mastered` `KnowledgeState` in a v2 payload). `BackupArchiveReader.ValidateVersionedAsync` peeks `formatVersion` (via the source-generated envelope, only after duplicate-property validation) and dispatches to the matching v1/v2 pipeline, rejecting anything outside `{1,2}` before touching `data.json`; `BackupArchiveReader.ValidateAsync` (v1-only) is unchanged. `BackupArchiveWriter` (v1) now takes a `ValidatedSchema7Capability` instead of reading `DatabaseSchema.CurrentVersion`, and no longer takes a redundant `BackupSnapshot` parameter — v1 JSON output is byte-identical.
    - **Deterministic v1→v2 upgrade**: the live migration's Sense-grouping and preferred-candidate-selection logic was extracted into a shared, connection-free `Schema8SemanticUpgradePolicy` (parameterized by an explicit source-order key — preserved local `Meaning.Id` for the live migration, ordinal v1 archive id for the upgrader — see the narrow architecture-doc correction in §5.1 of the design doc explaining why a v1 archive cannot always reconstruct the live migration's exact tie-break winner, only the same observable content). New `BackupArchiveV1UpgradePolicy.Upgrade(BackupPayload) -> BackupPayloadV2` and `DeterministicStableId` (fixed-namespace, SHA-256-based, never `Guid.NewGuid()`) with exact per-entity seeds (Meaning: vocabulary+archive-id+exact-variant-identity; Sense: vocabulary+semantic-identity-or-sentinel+sorted member fingerprints; AnswerVariant/Assignment: anchored to the parent Sense's StableId plus canonical semantic fields). Card `PreferredMeaningId` is copied directly from the v1 card's `PreparedItemId` (no re-derivation).
    - **Schema-8 capture/restore**: new `Data/Schema8/` (`Schema8BackupSnapshot`, `Schema8BackupSnapshotRepository`, `Schema8BackupImportRepository`, raw row models for the tables the migration changes) mirror `BackupSnapshotRepository`/`BackupImportRepository` without modifying any `Data/Entities` class. `BackupModelMapperV2` orders every collection by StableId/canonical content — never a local SQLite numeric id, even as a tiebreak. `Schema8BackupImportRepository.ImportIntoEmptySchema8Database` uses a reference-safe insertion order (Senses inserted with `DefaultMeaningId = NULL`, backfilled after Meanings exist, as its own fault-injectable checkpoint) and never reads or writes `PRAGMA user_version` (the capability resolver already validated it once, up front). A new type-safe capture envelope (`BackupSnapshotCapture`, `BackupMergeSafetyCopySnapshotCapture`) pairs each schema-specific snapshot with its matching capability type so a Schema-8 snapshot cannot reach the v1 writer or vice versa.
    - **Orchestration**: `BackupService.ImportPortableArchiveAsync` resolves schema capability inside the same `RunInTransactionAsync` callback that checks emptiness (no TOCTOU gap) and dispatches all four combinations — v1→Schema7 unchanged, v1→Schema8 upgrades in memory first, v2→Schema8 direct, v2→Schema7 rejected with the new stable code `schema8-archive-incompatible-with-schema7-target` and zero mutation. `IBackupService.ValidatePortableArchiveAsync` now returns a version-neutral `BackupPortableArchiveSummary` (all v1 counters + 4 nullable v2-only counters) built from either manifest version; verified safe against `Settings.razor`'s only call site, which discards the return value. `MergeSafetyCopyService` captures via the same type-safe envelope inside its existing race-free callback and validates with `ValidateVersionedAsync`; `MergeSafetyCopyResult`/`MergeSafetyCopyMetadata` were updated to the version-neutral summary/counts types.
    - **Tests**: 32 new focused tests (`KnownFirst.Tests/BackupArchiveV2Tests.cs`, `Schema8BackupRestoreTests.cs`, `Schema8BackupFixtureBuilders.cs`, plus 2 additions to `MergeSafetyCopyServiceTests.cs`) covering the ten tests-first core cases, format 0/3/impossible-version-mixing rejection, every graph-invariant violation case, StableId determinism/collision-resistance, empty restore of v1 and v2 into a synthetic empty Schema-8 database, `PRAGMA user_version` unchanged before/after success and after injected-failure rollback (first- and last-mutation checkpoints), v1-upgrade semantic parity against the live migration, and Schema-8 merge-safety-copy success/active-workflow-blocking. One pre-existing test (`PortableArchive_UnsupportedFormatIsRefusedBeforeMutation`) was corrected to mutate to format `3` instead of `2`, since `2` is now a real supported version. `Schema7Fixture` gained `InsertDocumentAsync`/`InsertLearningSessionAsync` helpers (previously-uncovered gaps found while building realistic fixtures). Full suite: 1084/1084 (1052 existing + 32 new).
    - **Binding-requirements correction pass** (same branch, still uncommitted): closed all four gaps the prior pass left open, none changing `Data/DatabaseSchema.cs`, any `Data/Entities` class, `CurrentVersion` (`7`), or `Components/Pages/Settings.razor`.
      - **No-discriminator Sense StableId collision test** (`Schema8BackupRestoreTests.V1Upgrade_NoDiscriminator_IdenticalVocabAndMeaningArchiveIdText_DifferentExactContent_ProducesDistinctDeterministicOrderIndependentSenseStableIds`): two independent v1 upgrades sharing identical Vocabulary/Meaning archive-id text, neither Sense carrying a reliable discriminator, differing only in exact Meaning content (`Translation`) — proves distinct, deterministic, collection-order-independent Sense `StableId`s, confirming the existing seed rule (entity tag + Vocabulary identity + semantic-identity-or-no-discriminator marker + ordinal-sorted member archive-id/`ExactMeaningVariantIdentity` pairs) already implemented in `BackupArchiveV1UpgradePolicy` resists this exact collision shape.
      - **Complete local-ID-independence test** (`BackupArchiveV2Tests.LocalIdIndependence_TwoEquivalentSchema8DatabasesWithScrambledLocalIds_ProduceByteIdenticalV2DataJson`): two Schema-8 databases built directly (bypassing the live migration's own local-`Meaning.Id`-based representative/preferred tie-break, a separate and intentional concern for that single-database migration, not this mapper/snapshot path) with identical `StableId`s and content but deliberately different local integer ids, insertion order, and (via a `sqlite_sequence` pre-seed covering every one of the 16 required tables, not just Documents/Words) displaced `AUTOINCREMENT` sequences across every major collection (Documents/SentenceSpans/WordOccurrences/Words/WordForms/Meanings/ContextSnapshots/Senses/AnswerVariants/Assignments/Cards/LearningSession+Reviews+SessionCards/ReviewSession+Candidate/PreparationSession+Candidate/AnswerVariantProgress). **Strengthened** (same branch, same pass): a new `AssertLocalIdsDifferAcrossEveryMajorCollectionAsync` helper runs *before* serialization and explicitly proves the premise table by table — locating the equivalent logical row in both databases via a stable/semantic key (StableId, fixed content, or a parent join, never row order) and asserting its local numeric id actually differs — rather than assuming the scrambling code worked. This caught a real gap in the test's own fixture construction (not in production code): the original scramble variant only displaced the Documents/Words sequences, so several single-row tables (starting with `WordForms`) still received identical local ids in both databases despite the byte-identical-output assertion still passing; corrected by displacing every table's sequence. Only *the two Schema8BackupFixtureBuilders-adjacent test files* changed — `BackupModelMapperV2` needed no correction, since re-running the strengthened test (now with proven, genuinely-different physical ids in every collection) still produces byte-for-byte identical raw `data.json` (never normalized after serialization).
      - **Named restore-failure checkpoints**: `Schema8BackupImportRepository.Checkpoints` (`AfterSenseInsertion`, `DuringMeaningVariantAssignmentInsertion`, `AfterDefaultMeaningBackfill`, `AfterCardsBeforeLearningHistory`, `BeforeCompletion`), fired via a new `IBackupImportFailureInjector.AtCheckpoint(string)` default-no-op method (existing `AfterMutation`-only injectors, v1 and v2, keep compiling and behaving unchanged). One test proves all five fire in stable relative order during a normal restore; five more (one per checkpoint) prove the injected exception occurs at that exact boundary, every durable Schema-8 table is empty afterward (no partially-backfilled `Sense.DefaultMeaningId`), `PRAGMA user_version` stays `8`, no stray database/WAL/SHM/journal/staging artifact remains next to the target database file, and a retry without injection succeeds. The existing raw mutation-count injectors/tests (first/last mutation) are retained unchanged as additional, non-exclusive proof.
      - **Model-contract coverage confirmed/added**: v2 rejecting `Prepared`/`Learning`/`Mastered` `KnowledgeState` was already covered by the existing `BackupArchiveV2Tests.BackupModelContractV2_SchemaEightWordStatus_RejectsLegacyPreparedLearningMastered` (all three values, one test) — not duplicated. v1 continuing to accept its legacy status values was already covered by existing v1 `BackupModelContractTests`/`BackupCreationTests` coverage — not duplicated. New: separate per-entity v2 record-count-mismatch tests for Senses, AnswerVariants, SenseAnswerVariantAssignments, and AnswerVariantProgress (`ValidateVersioned_V2Manifest*CountMismatch_RejectedSeparately`); a v2 source-material content-checksum-mismatch test that corrupts only the persisted `contentSha256` field (recomputing the outer archive checksum so the *inner* per-source-material check is the one actually exercised, not the outer one); and `ImportPortableArchive_V2ValidationFailure_LeavesTargetCompletelyUnmutated`, proving `ImportPortableArchiveAsync` validates the whole archive before its restore transaction ever opens (a corrupt archive reaches `ValidationFailed` with zero durable rows written, never `Failed`, which is reserved for a mid-restore rollback).
      - **Final evidence correction** (same branch, same pass): strengthened the local-ID-independence test as described above and re-verified the file-level repository inventory (20 modified + 20 added = 40 changed paths, 0 deleted, 0 renamed, all Slice 2). This pass edited an existing test method and its fixture-builder helper — it added zero new `[TestMethod]`s, so the complete-suite total is unaffected and stays `1098/1098` (last run confirmed, not rerun during this evidence-only pass; only the directly affected `BackupArchiveV2Tests`/`Schema8BackupRestoreTests` classes were re-run, both green).
      - **Test-count reconciliation**: the prior pass's arithmetic conflated two separately-confirmed baselines. The last independently-confirmed complete-suite run before Slice 2 was `1044/1044` (Slice 1 completion); a separate, uncommitted 8-test correction pass was layered on top without its own complete-suite run (noted at the time as "last known good: 1044/1044, unaffected by this internal-only correction") — so the real pre-Slice-2 baseline actually exercised was `1052` (`1044 + 8`), not `1044`, the first time the complete suite ran again. Slice 2's own first pass added 32 new tests in entirely new files/methods (30 in the two new files `BackupArchiveV2Tests.cs`/`Schema8BackupRestoreTests.cs` + 2 added to existing `MergeSafetyCopyServiceTests.cs`) and reported `1084/1084`, which is arithmetically `1052 + 32` — correct once the real baseline is used, but the complete suite had at that point already been run twice total (`1044`, then `1084`), never separately confirmed at the intermediate `1052` checkpoint. This correction pass added 14 further tests, all in the two existing Slice-2 test files (7 in `BackupArchiveV2Tests.cs`, 7 in `Schema8BackupRestoreTests.cs`; zero entirely-new test files, zero existing tests edited rather than added): final discovered complete-suite total is `1098/1098` (`1084 + 14`), confirmed by one complete-suite run after all corrections passed — the suite's third full run overall, and the first to actually pass through the `1052`/`1084`/`1098` checkpoints in sequence within one session.
  - **Slice 3 — multi-Sense preparation and topic persistence, dual-schema compatible** (branch `feature/meaning-centric-multi-sense-preparation-v1`, committed as `a51b0e82e35b2bb82d707580fe4fa04f8ac79765`, PR #33 open, not merged): completed the previously-partial Slice 3 implementation (§4.8.2). Schema 7's `PreparationService.StartAsync`/`AcceptAsync` selection/write behavior is unchanged (same persisted rows, same decisions), but the code itself was refactored, not left byte-for-byte identical (see [kf-meaning-slice3-complete-handoff.md](handoffs/kf-meaning-slice3-complete-handoff.md) §D item 3); Schema 8 gained a real candidate-selection/eligibility path (not just an already-dormant Accept path).
    - **Neutral schema capability**: `Services/Study/PreparationSchemaCapability.cs` (`ValidatedPreparationSchema7/8Capability`, `PreparationSchemaCapabilityException`) reuses the existing `Schema8ShapeValidator` but is fully independent of `Services/DataSafety/BackupSchemaCapability` — preparation no longer depends on a backup-specific capability type or exception. `BackupSchemaCapability` itself is unchanged.
    - **Shared context-evidence policy**: `KnownFirst.Core.Preparation.PreparationContextEvidencePolicy` (new, database-independent) holds the normalization/SHA-256-fingerprint/four-field-key (`ContextEvidenceKey`: SourceDocumentId, NormalizedFingerprint, TargetStart, TargetLength) algorithm extracted verbatim from the pre-Slice-3 `PreparationService.NormalizeContext`/`CreateFingerprint`; the Schema-7 context path now delegates to it (byte-identical output, covered by a frozen-reference-implementation parity test) and the new Schema-8 evidence scanner uses the same policy.
    - **Real Schema-8 `StartAsync`** (`PreparationServiceSchema8Start.cs`): a Word already `PreparationState.Prepared` with existing Senses is eligible again only when `Schema8EvidenceScanner.HasGenuinelyNewEvidence` (an unbounded (DocumentId, Order)-ordered scan, never capped at three) finds a context outside the Word's `Schema8EvidenceLedger.ComputeEffectiveProcessedKeys` set; frequency remains ordering-only. Only after a Word survives priority/limit selection are up to three genuinely-new evidence snapshots frozen into a `PreparationCandidatePayloadV1` (`Result` now nullable — a "Pending, evidence already frozen, no lookup yet" envelope) while the candidate is still Pending.
    - **Effective-processed-evidence ledger** (`Schema8EvidenceLedger.cs`): implements the documented `legacyBaselineKeys ∪ fullyResolvedPreparedEnvelopeKeys` formula exactly (Empty/LegacyLexicalResult candidate history owns no envelope evidence; Skipped/Cancelled/Failed/Pending/ResultReady/partially-resolved envelopes mark nothing as fully processed); malformed/unsupported candidate history throws the new `PreparationCandidateStateException` before `StartAsync` creates any session/candidate.
    - **Frozen candidate evidence**: `PreparationService.CreateItemAsync` now branches on the ResultJson shape — a genuine EnvelopeV1 resolves its `Contexts` from `FrozenEvidence` (re-matched against a live occurrence scan by the same four-field key; documents/occurrences are immutable so this is exact, never a fresh ledger-driven scan), so `Contexts[0]` is always the frozen first context sent to the lexical provider; Schema-7/Empty/Legacy candidates keep the exact prior live-scan algorithm. `AcceptSchema8` now links only the candidate's own frozen evidence (`ResolveContextDataFromFrozenEvidence`) and never recomputes or replaces it — a later-imported occurrence never changes an active candidate's evidence.
    - **Lazy envelope upgrade** (`PreparationService.EnsureCandidateEnvelopeAndSelection`, called from `GetCurrentAsync`, `LookupCurrentAsync`, `SelectMeaningAsync`, and `AcceptSchema8`): a genuine EnvelopeV1 is never rewritten; Empty becomes an envelope with `Result = null` plus newly frozen evidence; a raw `LegacyLexicalResult` becomes an envelope wrapping that exact result plus newly frozen evidence, with a valid `SelectedMeaningIndex` preserved or deterministically clamped; unsupported/malformed data throws before any write; no network lookup is ever triggered by an upgrade (read-only code paths peek cheaply first and only open a transaction when something must change).
    - **Provider-index integrity**: `AcceptSchema8` re-reads `SelectedMeaningIndex` fresh inside its transaction, rejects out-of-range/already-resolved indexes, and canonically compares a non-empty `input.SelectedMeaningId` against the persisted `Result.Meanings[selectedIndex].MeaningId` (stale selections fail before mutation). `SelectMeaningAsync` rejects out-of-range and already-resolved indexes. `PreparationCandidatePayloadCodec.Write` already enforced strictly-ascending, duplicate-rejecting ledger serialization (unchanged).
    - **Metadata persistence**: `PreparationMetadataPolicy.cs` (trim, Unicode Form C, empty→`string.Empty`, 256/128-byte UTF-8 limits for TopicOrDomain/PartOfSpeech, `PreparationMetadataValidationException`) is validated before any mutation in both `AcceptSchema7` (validated, never persisted, never changes legacy rows) and `AcceptSchema8` (persisted onto the Sense). PartOfSpeech resolution order: explicit input, else the selected provider Meaning, else empty.
    - **All-exact/multi-Meaning lifecycle**: in addition to the existing post-explicit-accept opportunistic all-exact auto-linking, `LookupCurrentAsync`'s persistence transaction now also auto-resolves every provider index that matches an existing Sense immediately after a successful lookup (before any explicit accept), advances `SelectedMeaningIndex` to the lowest still-unresolved index, and auto-completes the candidate (`PreparationState.Prepared`, session counters updated exactly once) when that resolves every index — a repeated `LookupCurrentAsync`/`GetCurrentAsync` call no longer finds an auto-completed candidate "current" and cannot double-complete it.
    - **Fault injection**: `IPreparationFaultInjector`/`PreparationSchema8Checkpoints` now name the nine required boundaries (`AfterEnvelopePersist`, `AfterSenseInsert`, `AfterMeaningInsert`, `AfterContextLink`, `AfterCardInsert`, `AfterResolvedIndexPersist`, `DuringAutoExactVariantLinking`, `BeforeCandidateCompletion`, `BeforeAutomaticCandidateCompletion`), each proven (parameterized test) to roll back completely, leave `PRAGMA user_version` at 8, and allow a clean retry.
    - **Tests**: `PreparationSchemaCapabilityTests.cs`, `PreparationContextEvidencePolicyTests.cs`, `PreparationServiceSchema8StartAndEvidenceTests.cs`, `PreparationServiceSchema8LazyUpgradeTests.cs`, `PreparationMetadataPolicyTests.cs`, `PreparationProviderIndexIntegrityTests.cs` added; `PreparationCandidatePayloadCodecTests.cs`/`PreparationServiceSchema8AcceptTests.cs` updated for the nullable-`Result` envelope shape and renamed checkpoints. Every Schema-8 fixture remains synthetic (`TemporarySchema8Database`/`Schema7Fixture` + `Schema8DormantMigration`), never a real application database; `DatabaseSchema.CurrentVersion` stays `7`. Complete suite: 1190/1190 (1144 + 46).
    - No `Data/DatabaseSchema.cs`, `Data/Entities/*`, `Schema8Ddl`, `Components/Pages/PrepareWords.razor`, localization, archive DTO/format, `LearningService`, answer-variant, or package/build/secret/branding change.

## New-chat handoff

"Read AGENTS.md, docs/PROMPT_AND_TASK_ROUTING.md, docs/CURRENT_WORK.md, and docs/INDEX.md completely. Verify branch, HEAD, Git status, and registered worktrees. Follow the task-specific reading route from docs/INDEX.md. Then perform only the task under 'Next exact action'."
